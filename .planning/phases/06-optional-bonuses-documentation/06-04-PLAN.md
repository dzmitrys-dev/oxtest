---
phase: 06-optional-bonuses-documentation
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - README.md
  - ONBOARDING.md
autonomous: true
requirements: [DOC-01, DOC-02]
tags: [docs, readme, onboarding, interview-prep]

must_haves:
  truths:
    - "README.md is runnable from the README alone: docker-compose-first primary run path (docker compose up → redis + api + worker + served React UI on :3000) with local dev as a secondary section (D-12)"
    - "README includes the criterion #3 MANDATORY checklist: copy-paste run (compose + local dev), the memory self-test command, the assignment-level acceptance command, an architecture overview, the NodeGoat scan demo, and the honest index.js-vs-worker.js self-test mapping (D-15)"
    - "Every command cited in README is a REAL existing script/command (from apps/api/package.json, docker-compose.yml, and the graded verbatim command) — no invented commands (D-15)"
    - "README's architecture overview is an ASCII diagram + brief prose (no mermaid/render dependency), and links to ONBOARDING for the deep why (D-13, D-14)"
    - "README demonstrates a real functional scan against the OWASP NodeGoat repository (default upstream https://github.com/OWASP/NodeGoat) with an explicit 'substitute your fork' note (D-15a)"
    - "README explains the two-entrypoint self-test honesty: node --max-old-space-size=150 dist/index.js boots the API, while the 500MB+ stream-parse runs in dist/worker.js; the standalone parser memtest is the honest 500MB proof (D-15b)"
    - "ONBOARDING.md explains every implemented solution as What/Why/How + an explicit 'A reviewer might ask…' Q&A block per topic (D-09)"
    - "ONBOARDING documents rejected alternatives/trade-offs per topic and explicitly owns the NestJS-vs-Fastify tension and the GraphiQL/introspection exposure trade-off (D-10, Pitfall 6)"
    - "ONBOARDING covers all D-11 topics: memory strategy, architecture layering, queue design, error handling, type safety, streaming/backpressure, Trivy local-detect+Docker-fallback & socket-mount trade-off, two-entrypoint + self-test honesty, guaranteed try/finally cleanup, and testing strategy (the @nestjs/bullmq+@swc Jest landmine → compiled-dist + node:test)"
    - "README runs, ONBOARDING explains — no duplication; single source of truth per concern (D-14)"
  artifacts:
    - "README.md — reviewer run guide (DOC-01)"
    - "ONBOARDING.md — interview-prep What/Why/How + Q&A (DOC-02)"
  key_links:
    - "README run commands → real apps/api/package.json scripts + docker-compose.yml + graded verbatim command"
    - "ONBOARDING topics → rejected-alternative rationale harvested from Phase 1-5 CONTEXT/STATE (RESEARCH Documentation Source Map)"
---

<objective>
Ship the two REQUIRED documentation deliverables. README.md (DOC-01) gives a reviewer a copy-paste, runnable-from-the-README-alone guide led by `docker compose up` (now serving the React UI + GraphQL + REST on one port), with the mandatory criterion #3 checklist, an ASCII architecture diagram, a real NodeGoat scan demo, and the honest index.js-vs-worker.js self-test mapping. ONBOARDING.md (DOC-02) is interview-prep: every implemented solution as What/Why/How + a skeptical-reviewer Q&A, with rejected alternatives owned per topic — including the NestJS-vs-Fastify tension and the GraphiQL exposure trade-off. README runs; ONBOARDING explains; no duplication (D-14).

Purpose: satisfy phase success criteria #3 (README) and #4 (ONBOARDING) and requirements DOC-01/DOC-02.
Output: README.md and ONBOARDING.md at the repo root.
</objective>

<execution_context>
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/workflows/execute-plan.md
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-optional-bonuses-documentation/06-CONTEXT.md
@.planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md
@apps/api/package.json
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md — docker-compose-first run guide + criterion #3 checklist</name>
  <files>README.md</files>
  <read_first>
    - apps/api/package.json (AUTHORITATIVE script names to cite verbatim: memtest, memtest:sweep, gen:fixture, test:selftest, test:acceptance, test:oom:container, build, start:api, start:worker, dev:api, dev:worker)
    - docker-compose.yml (the three services + ports + command lines; api serves the UI after Plan 03)
    - Dockerfile (multi-stage build the README references)
    - .planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md ("README existing commands to cite" table; NodeGoat demo target; honest self-test explanation; Documentation Source Map)
    - .planning/phases/05-packaging-ops-assignment-acceptance/05-CONTEXT.md (self-test honesty D-10; socket-mount trade-off D-06)
    - .planning/ROADMAP.md (Phase 6 success criterion #3 — the exact checklist)
  </read_first>
  <action>
    Write README.md at the repo root. Lead with a one-paragraph project summary (Trivy-wrapping supply-chain scanner; the 500MB+/150MB-heap memory constraint is the pass/fail axis) and the evaluation-relevant highlights.

    Primary run path (D-12): `docker compose up` as the single one-command path — bring up redis + api + worker + the served React UI; state the reviewer opens http://localhost:3000 for the SPA, that /graphiql is the interactive GraphQL playground, and that REST lives at /api/scan and /health. Confirm no host-side Trivy/Redis install is needed (Docker socket-mounted sibling scanner, D-06). Secondary section: local dev (npm ci, npm run build, start:api / start:worker; dev:api / dev:worker with tsx watch; dev SPA via `npm run dev --workspace apps/web` with the Vite /graphql proxy to :3000). CITE ONLY real commands from apps/api/package.json and docker-compose.yml — do not invent script names.

    Criterion #3 MANDATORY checklist (D-15) — all must appear:
    (1) copy-paste run for BOTH compose and local dev;
    (2) the memory self-test command (`npm run memtest --workspace apps/api`, plus the sweep `npm run memtest:sweep`, and the fixture generator `npm run gen:fixture`);
    (3) the assignment-level acceptance command (`npm run test:acceptance --workspace apps/api`);
    (4) an architecture overview as an ASCII diagram + brief prose (D-13) — show browser → API process (SPA static + GraphQL + REST) → BullMQ/Redis → worker (clone → Trivy → stream-parse → CRITICAL → Redis, cleanup); note GraphQL + SPA are API-process-only and the worker is the memory-critical process; link to ONBOARDING for the deep why (D-14);
    (5a) a REAL functional scan demo against OWASP NodeGoat — use the default upstream URL https://github.com/OWASP/NodeGoat with an explicit "substitute your own fork here" note (do NOT block on a user-provided fork URL; the default upstream + substitution note satisfies D-15a);
    (5b) the honest self-test mapping (D-15b): the PDF's verbatim `node --max-old-space-size=150 dist/index.js` boots the API (which itself does not parse 500MB), while the 500MB+ stream-parse runs in `dist/worker.js`; the standalone parser memtest against the 500MB fixture is the honest 500MB proof. Reference `npm run test:selftest` (criterion #5a) for the verbatim-command boot proof.

    Keep README run-focused: short architecture overview + a link to ONBOARDING for rationale; do NOT duplicate the What/Why/How that lives in ONBOARDING (D-14). Do not embed any secret values — reference env var NAMES only (T-06-09).
  </action>
  <verify>
    <automated>test -f README.md && grep -q 'docker compose up' README.md && grep -q 'memtest' README.md && grep -q 'test:acceptance' README.md && grep -q 'NodeGoat' README.md && grep -q 'dist/worker.js' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README leads with `docker compose up` as the primary path and has a secondary local-dev section; the reviewer can run the stack from the README alone (D-12).
    - The criterion #3 checklist is complete: run instructions (compose + local), memory self-test command, acceptance command, ASCII architecture overview, NodeGoat demo (default upstream + substitute-your-fork note), and the honest index.js-vs-worker.js mapping (D-15/a/b).
    - Every cited command exists in apps/api/package.json or docker-compose.yml or is the graded verbatim `node --max-old-space-size=150 dist/index.js` — no invented commands.
    - Architecture is an ASCII diagram + prose (no mermaid); it links to ONBOARDING and does not duplicate ONBOARDING's rationale (D-13/D-14).
    - No secret values appear (env var names only, T-06-09).
  </acceptance_criteria>
  <done>README.md is a runnable-from-the-README-alone, compose-first guide containing the full criterion #3 checklist with only real commands, an ASCII architecture diagram, the NodeGoat demo, and the honest self-test mapping.</done>
</task>

<task type="auto">
  <name>Task 2: ONBOARDING.md — What/Why/How + reviewer Q&A across all topics</name>
  <files>ONBOARDING.md</files>
  <read_first>
    - .planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md (Documentation Source Map — the topic→rationale-source table; Pitfall 6 GraphiQL trade-off; NestJS-vs-Fastify tension)
    - .planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md (memory + streaming/backpressure rationale; fs.readFile/JSON.parse and bfj-vs-stream-json rejections)
    - .planning/phases/03-scan-engine-adapters-queue-worker-service/03-CONTEXT.md (queue design, adapters, try/finally cleanup, bounded error reasons)
    - .planning/phases/04-required-rest-api-runtime-lifecycle/04-CONTEXT.md (architecture layering, thin-controller import-guard)
    - .planning/phases/05-packaging-ops-assignment-acceptance/05-CONTEXT.md (node:22-slim over alpine/distroless, socket-mount trade-off D-06, self-test honesty D-10)
    - .planning/STATE.md (the @swc/core + @nestjs/bullmq miette Jest landmine → compiled-dist + node:test; TS 6.0.3 pin; logging/scanId; config fail-closed)
    - .claude/CLAUDE.md (the Fastify-over-NestJS recommendation to OWN per D-10)
  </read_first>
  <action>
    Write ONBOARDING.md at the repo root as interview-prep. For EACH topic below use the D-09 structure: a What section, a Why section, a How section, an explicit "A reviewer might ask…" Q&A block, and a rejected-alternatives / trade-offs note harvested from the Phase 1-5 CONTEXT/STATE sources (D-10) — do not re-derive; cite the recorded rationale.

    Topics (all D-11 required):
    - Memory strategy (150MB heap ceiling, flat RSS regardless of input size, the pass/fail axis).
    - Streaming / backpressure (stream-json deep-leaf Pick + object-by-object; reject fs.readFile/JSON.parse; bfj vs stream-json; JSONStream superseded).
    - Architecture layering (thin Controller/Resolver → shared ScanService → injectable adapters; ARCH-01/02/03; the import-guard discipline).
    - Queue design (BullMQ concurrency:1, restart survival, SCAN_QUEUE Symbol token keeping @nestjs/bullmq out of the Jest graph, AsyncLocalStorage rejected for the Redis hop).
    - Error handling (Trivy exit codes: vulns-found is success; clone/disk-full/parse each mark Failed with a bounded {category, detail} reason).
    - Type safety (strict, noUncheckedIndexedAccess, no `any`; @ObjectType() classes doubling as typed models; TS pinned 6.0.3, verbatimModuleSyntax omitted for Nest DI).
    - Trivy local-detect + Docker fallback AND the compose socket-mount trade-off (Phase 5 D-06) — own it as an accepted single-tenant take-home trade-off.
    - Two-entrypoint topology + self-test honesty (index.js boots API; worker.js runs the 500MB parse; standalone memtest is the honest proof — Phase 5 D-10).
    - Guaranteed try/finally cleanup (clone dir + report file deleted on success and every failure path).
    - Testing strategy (the @nestjs/bullmq + @swc miette Jest landmine → compiled-dist + node:test harnesses; mock-only unit specs).
    - Bonus B GraphQL: code-first MercuriusDriver on the same Fastify process; the resolver reuses parseGithubUrl (SSRF parity); and OWN the GraphiQL/introspection-in-all-envs exposure as a deliberate, documented demo trade-off you'd gate behind env+auth in production (Pitfall 6, D-05).
    - Bonus A frontend: urql SPA served same-origin; the 2s poll; the table maps only the 6 stored Vulnerability fields.

    Explicitly OWN the NestJS-vs-Fastify tension (D-10): .claude/CLAUDE.md recommends Fastify-over-NestJS, yet the build is NestJS on the Fastify adapter — explain the decision (Module/Controller/Provider IS the graded Controller/Service/Worker separation; the Fastify adapter keeps it lean; GraphQL/SPA/REST share one Fastify listener) rather than leaving it unexamined.

    Keep ONBOARDING the single source of truth for the "why"; do not duplicate README's run steps (D-14). No secret values (T-06-09).
  </action>
  <verify>
    <automated>test -f ONBOARDING.md && grep -qi 'reviewer might ask' ONBOARDING.md && grep -qi 'Fastify' ONBOARDING.md && grep -qi 'GraphiQL' ONBOARDING.md && grep -qi 'landmine\|@nestjs/bullmq' ONBOARDING.md</automated>
  </verify>
  <acceptance_criteria>
    - Every topic uses What/Why/How + an "A reviewer might ask…" Q&A block (D-09).
    - All D-11 topics are present (memory, streaming/backpressure, architecture, queue, error handling, type safety, Trivy fallback + socket-mount, two-entrypoint + self-test honesty, try/finally cleanup, testing/Jest-landmine) plus the two bonuses.
    - Rejected alternatives/trade-offs are stated per topic and harvested from the Phase 1-5 sources (D-10); the NestJS-vs-Fastify tension and the GraphiQL/introspection trade-off are explicitly owned (Pitfall 6).
    - ONBOARDING holds the rationale (single source of truth) and does not restate README's run steps (D-14); no secret values (T-06-09).
  </acceptance_criteria>
  <done>ONBOARDING.md explains every implemented solution as What/Why/How + reviewer Q&A across all D-11 topics and both bonuses, owning the rejected alternatives and the NestJS-vs-Fastify and GraphiQL trade-offs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → public reviewer | README/ONBOARDING are published in a public GitHub repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Info Disclosure | Secrets / internal paths leaking into README or ONBOARDING | medium | mitigate | Docs cite env var NAMES only (never values); no secrets exist in the repo; .env is git/docker-ignored. Review both files for any accidental token/host/credential before finalizing. |
| T-06-02 | Info Disclosure | Documenting the accepted GraphiQL/introspection exposure (D-05) | low | accept | Honest disclosure is the goal — ONBOARDING states it is a deliberate single-tenant demo trade-off you'd gate behind env+auth in production (Pitfall 6). No new runtime surface is introduced by the docs. |
</threat_model>

<artifacts_produced>
New files this plan produces:
- `README.md` — reviewer run guide (compose-first, criterion #3 checklist, ASCII architecture, NodeGoat demo, honest self-test mapping) — DOC-01
- `ONBOARDING.md` — interview-prep What/Why/How + reviewer Q&A across all D-11 topics + both bonuses, owning rejected alternatives and the NestJS-vs-Fastify / GraphiQL trade-offs — DOC-02
</artifacts_produced>

<verification>
- README.md exists; contains `docker compose up`, `memtest`, `test:acceptance`, `NodeGoat`, and the `dist/worker.js` honest-mapping reference; all cited commands are real.
- ONBOARDING.md exists; every topic has an "A reviewer might ask…" block; the Jest landmine, GraphiQL trade-off, and Fastify tension are covered.
- No secret values in either file.
- No duplication of run steps between README and ONBOARDING (D-14).
</verification>

<success_criteria>
README gives copy-paste, runnable-from-the-README-alone instructions with the full criterion #3 checklist (criterion #3), and ONBOARDING explains every implemented solution in What/Why/How + reviewer Q&A form (criterion #4) — satisfying DOC-01 and DOC-02.
</success_criteria>

<output>
Create `.planning/phases/06-optional-bonuses-documentation/06-04-SUMMARY.md` when done.
</output>
