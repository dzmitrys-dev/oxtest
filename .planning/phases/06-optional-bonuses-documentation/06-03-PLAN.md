---
phase: 06-optional-bonuses-documentation
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - apps/api/src/app.module.ts
  - apps/api/package.json
  - apps/api/scripts/serve-static-routes.smoke.mjs
  - Dockerfile
  - docker-compose.yml
  - package.json
autonomous: true
requirements: [FE-01, FE-02, FE-03]
tags: [serve-static, fastify, docker, compose, packaging, bonus-a]

must_haves:
  truths:
    - "The API (Fastify) serves the built React SPA (apps/web/dist) as static assets from origin root GET / — one URL for the reviewer, no CORS (D-04)"
    - "The SPA static server does NOT shadow the backend routes: GET /health, GET/POST /graphql, GET /graphiql, and /api/scan/* still reach their handlers (not index.html), via a ServeStaticModule exclude list (Pitfall 4, T-06-03)"
    - "Static serving cannot path-traverse outside apps/web/dist (T-06-04)"
    - "node dist/index.js STILL boots to 'API HTTP listener ready' even when the SPA has not been built, because the api build always creates dist/web (real bundle or placeholder index.html) — the always-required criterion #5a self-test never regresses (T-06-08)"
    - "docker compose up serves the built UI alongside the API on :3000 — the built SPA is folded into the multi-stage Dockerfile so no host-side build is required (D-04, D-12)"
    - "GraphQL/static are registered in AppModule only — the worker image/process never loads them (two-entrypoint heap discipline)"
  artifacts:
    - "apps/api/src/app.module.ts — ServeStaticModule.forRoot registration with exclude list"
    - "apps/api/scripts/serve-static-routes.smoke.mjs — route-exclusion + SPA-served smoke harness"
    - "Dockerfile — web build folded into the builder stage; apps/web/dist copied into apps/api/dist/web"
    - "root/api build scripts producing apps/api/dist/web"
  key_links:
    - "ServeStaticModule.rootPath = join(__dirname, 'web') → apps/api/dist/web at node dist/index.js runtime"
    - "Dockerfile builder: build apps/web → copy apps/web/dist → apps/api/dist/web → flows into runtime via existing dist COPY"
    - "docker-compose api service (working_dir /app/apps/api, command node dist/index.js) serves dist/web"
---

<objective>
Wire the Bonus A SPA (Plan 02) to be served as static assets by the API on the same origin (D-04), and fold its build into the multi-stage Dockerfile so `docker compose up` serves the UI alongside REST + GraphQL on one port (D-12). Register `ServeStaticModule` in `AppModule` ONLY (worker heap stays lean) with an `exclude` list so the SPA catch-all never shadows `/api`, `/graphql`, `/graphiql`, or `/health` (Pitfall 4), and guarantee `node dist/index.js` still boots when the SPA is unbuilt so the always-required criterion #5a self-test never regresses.

Purpose: complete the reviewer-accessible delivery of FE-01/FE-02/FE-03 — the SPA is reachable at one URL via `docker compose up`.
Output: `AppModule` static-serving, a route-exclusion smoke harness, Dockerfile web-build fold-in, build-script wiring, and a compose served-UI note.
</objective>

<execution_context>
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/workflows/execute-plan.md
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-optional-bonuses-documentation/06-CONTEXT.md
@.planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md
@apps/api/scripts/selftest-index-boot.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ServeStaticModule with route exclusions + a boot-safe dist/web + route smoke</name>
  <files>apps/api/src/app.module.ts, apps/api/package.json, apps/api/scripts/serve-static-routes.smoke.mjs</files>
  <read_first>
    - apps/api/src/app.module.ts (current AppModule — after Plan 01 it has GraphQLModule; add ServeStaticModule)
    - apps/api/package.json (build script: `nest build && tsc ...`; nest-cli deleteOutDir:true WIPES dist on every build)
    - apps/api/nest-cli.json (deleteOutDir:true — why dist/web must be (re)created AFTER nest build)
    - apps/api/scripts/selftest-index-boot.mjs (the criterion #5a boot harness that must keep passing; mirror its Redis-less boot idiom for the new smoke)
    - .planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md (Pattern 2 ServeStaticModule; Pitfall 3 rootPath; Pitfall 4 exclude token; Open Questions 1-2)
  </read_first>
  <action>
    Install @nestjs/serve-static@5.0.5 into apps/api (peer @fastify/static is already provided transitively by mercurius@16.9.0).

    Modify apps/api/src/app.module.ts: add `ServeStaticModule.forRoot({ rootPath: join(__dirname, 'web'), exclude: [...] })` to imports (import ServeStaticModule from @nestjs/serve-static, join from node:path). rootPath join(__dirname,'web') resolves to apps/api/dist/web at `node dist/index.js` runtime (Pitfall 3). The exclude list MUST bypass all four backend route groups so the SPA never shadows them (Pitfall 4): the /api scan routes, /health, /graphql, and /graphiql. Wire an initial exclude using the path-to-regexp v8 wildcard form (e.g. '/api/{*path}', '/health', '/graphql', '/graphiql') and then EMPIRICALLY confirm the token via the Task-created smoke — if any backend route returns SPA HTML, adjust the wildcard token (Open Question 1) until all four bypass. Keep ServeStaticModule in AppModule ONLY; do not touch worker.module.ts.

    Make the API boot-safe when the SPA is unbuilt (T-06-08): update the apps/api build so that AFTER `nest build` (which deletes dist via deleteOutDir), a step ensures apps/api/dist/web/index.html exists — copy apps/web/dist into apps/api/dist/web when it exists, otherwise write a minimal placeholder index.html (a one-line "UI not built" page). This guarantees ServeStaticModule's rootPath always exists so `node dist/index.js` boots even in the Docker-free CI contexts (memory.yml, scan-engine.yml) that build the API but not the web app. Implement this as an npm script (e.g. a `postbuild` or an explicit `build:web-into-dist` step invoked by the api build) — but do NOT make it depend on apps/web being built (those CI jobs must still pass).

    Create apps/api/scripts/serve-static-routes.smoke.mjs mirroring selftest-index-boot.mjs's Redis-less boot idiom (node:test + assert/strict; allocate then release an ephemeral loopback port to use as a CLOSED REDIS_PORT; spawn dist/index.js with shell:false argv; wait bounded for the 'API HTTP listener ready' marker; status-preserving finally teardown that kills the process). Once booted, assert: GET / returns HTML containing the SPA root div (served from dist/web); GET /health returns a JSON body (not HTML) — 503 without Redis is acceptable, the point is it is NOT shadowed by the SPA; GET /graphiql returns 200 HTML that is the GraphiQL playground (not the SPA); POST /graphql with a trivial introspection query returns a GraphQL JSON response; GET /api/scan/does-not-exist returns 404 JSON (not SPA HTML). Add an npm script test:serve-static to run it.
  </action>
  <verify>
    <automated>npm run build --workspace apps/api && test -f apps/api/dist/web/index.html && npm run test:selftest --workspace apps/api && node --test apps/api/scripts/serve-static-routes.smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - After `npm run build --workspace apps/api`, apps/api/dist/web/index.html exists (real bundle if apps/web/dist present, else placeholder) — so `node dist/index.js` boots and test:selftest passes (criterion #5a never regresses, T-06-08).
    - serve-static-routes.smoke.mjs passes: GET / serves SPA HTML; GET /health, POST /graphql, GET /graphiql, GET /api/scan/* all bypass the SPA (reach their handlers) — the exclude token is confirmed empirically (T-06-03, Pitfall 4).
    - ServeStaticModule is registered in AppModule only; worker.module.ts unchanged (heap discipline).
    - @fastify/static (via ServeStaticModule) serves only apps/web/dist and rejects path traversal (T-06-04) — no route serves files outside dist/web.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The API serves the SPA at / with the four backend route groups excluded and verified by an automated smoke; the api build always yields a boot-safe dist/web; the criterion #5a self-test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fold the web build into the Dockerfile + docker-compose served-UI note</name>
  <files>Dockerfile, docker-compose.yml, package.json</files>
  <read_first>
    - Dockerfile (multi-stage: builder runs `npm run build --workspace apps/api`; runtime copies /app/apps/api/dist)
    - docker-compose.yml (api service: working_dir /app/apps/api, command node dist/index.js, port 3000; three services total)
    - package.json (root — build script currently `npm run build --workspace apps/api`)
    - .planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md (Open Question 2 canonical dist/web location; Runtime State Inventory — apps/web/dist build artifact)
  </read_first>
  <action>
    Modify the Dockerfile builder stage so the web bundle is produced and placed where the runtime serves it. Because `npm ci` at the root already installs all workspaces once apps/web is registered (Plan 02), add a builder step to build the web app (`npm run build --workspace apps/web`, emitting apps/web/dist) BEFORE `npm run build --workspace apps/api`, then AFTER the api build copy apps/web/dist into apps/api/dist/web (nest build's deleteOutDir wipes dist, so the copy MUST follow the api build — Open Question 2). The existing runtime-stage line `COPY --from=builder /app/apps/api/dist ./apps/api/dist` then carries dist/web into the final image unchanged — do not add a second runtime COPY. Keep the runtime stage prod-only deps: apps/web devDeps (vite etc.) live only in the builder stage and never enter the runtime image. If the api build's boot-safe copy step (Task 1) already lands apps/web/dist into dist/web when present, prefer reusing it over a duplicate Dockerfile cp — keep exactly one mechanism, documented in a Dockerfile comment.

    Update the root package.json build script (and/or add a `build:all`) so a single command builds web + api and yields apps/api/dist/web for the local `node dist/index.js` served-UI path — without breaking the api-only `npm run build --workspace apps/api` used by existing CI.

    Update docker-compose.yml with a brief comment on the api service documenting that it now also serves the built React SPA at / from dist/web (same origin, D-04) — keep the service count at exactly three (redis + api + worker); the worker service is unchanged and must NOT serve or build the UI (heap discipline). Do not add a new service or a second port.
  </action>
  <verify>
    <automated>docker build --target runtime -t code-guardian-app:phase6-web . && docker run --rm --entrypoint sh code-guardian-app:phase6-web -c 'test -f /app/apps/api/dist/web/index.html && echo WEB_OK'</automated>
  </verify>
  <acceptance_criteria>
    - The runtime image contains /app/apps/api/dist/web/index.html (the real Vite bundle, not the placeholder) — the SPA is served by `docker compose up` at :3000 / (D-04, D-12).
    - The web build runs in the builder stage only; the runtime image carries no web devDependencies (image stays lean).
    - docker-compose.yml still defines exactly three services (redis, api, worker); the worker is unchanged and serves no UI.
    - The api-only `npm run build --workspace apps/api` still succeeds for existing CI (does not require apps/web to be pre-built).
  </acceptance_criteria>
  <done>The multi-stage Dockerfile builds and bundles the SPA into apps/api/dist/web; `docker build` yields an image serving the UI at /; compose documents the served UI and keeps three services with an unchanged worker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → static file server | Arbitrary paths (incl. traversal attempts) hit the SPA static handler |
| SPA catch-all ↔ backend routes | The static server must not intercept /api, /graphql, /graphiql, /health |
| Build → boot | A missing SPA bundle must not crash the always-required self-test boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | SPA catch-all shadowing /api, /graphql, /graphiql, /health | high | mitigate | ServeStaticModule `exclude` list bypasses all four backend route groups; the exclude token is confirmed empirically by serve-static-routes.smoke.mjs (Pitfall 4). |
| T-06-04 | Info Disclosure | Static path traversal (GET /../../etc/passwd) | medium | mitigate | @fastify/static (under ServeStaticModule) normalizes and rejects traversal and serves only apps/web/dist; no handler exposes files outside dist/web (ASVS V12). |
| T-06-08 | Denial of Service | ServeStaticModule missing rootPath crashes boot → breaks the always-required criterion #5a self-test | medium | mitigate | The api build always (re)creates apps/api/dist/web (real bundle or placeholder index.html) after nest build's deleteOutDir; test:selftest verifies dist/index.js still boots. |
| T-06-SC | Tampering | npm install (@nestjs/serve-static) | high | mitigate | Install the verbatim pinned version (RESEARCH Standard Stack, Approved in the Package Legitimacy Audit — no [SLOP]/[ASSUMED], no postinstall); no host tool added; runtime stays prod-only deps. |
</threat_model>

<artifacts_produced>
New/modified artifacts this plan produces:
- `apps/api/src/app.module.ts` — ServeStaticModule.forRoot({ rootPath: join(__dirname,'web'), exclude:[...] })
- `apps/api/scripts/serve-static-routes.smoke.mjs` — route-exclusion + SPA-served boot smoke (+ `test:serve-static` script)
- api build step producing a boot-safe `apps/api/dist/web/index.html` (real or placeholder)
- Dockerfile builder: web build + copy of apps/web/dist → apps/api/dist/web
- root `build`/`build:all` producing the served-UI bundle for local `node dist/index.js`
- docker-compose.yml served-UI comment (still three services)
- Pinned dep: @nestjs/serve-static@5.0.5
</artifacts_produced>

<verification>
- `npm run build --workspace apps/api` → apps/api/dist/web/index.html exists; `npm run test:selftest` passes (criterion #5a).
- `node --test apps/api/scripts/serve-static-routes.smoke.mjs` — GET / serves SPA; /health, /graphql, /graphiql, /api/* all bypass the SPA (T-06-03).
- `docker build --target runtime` yields an image with /app/apps/api/dist/web/index.html (real bundle).
- docker-compose.yml has exactly three services; worker unchanged.
- worker.module.ts still has no GraphQL/static imports.
</verification>

<success_criteria>
The built React SPA is served by the API at origin root with all four backend route groups excluded and verified; `docker compose up` serves the UI on :3000 alongside REST + GraphQL; the criterion #5a self-test never regresses; the worker heap discipline is preserved.
</success_criteria>

<output>
Create `.planning/phases/06-optional-bonuses-documentation/06-03-SUMMARY.md` when done.
</output>
