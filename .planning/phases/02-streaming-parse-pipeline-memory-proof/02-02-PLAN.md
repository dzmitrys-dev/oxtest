---
phase: 02-streaming-parse-pipeline-memory-proof
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [apps/api/scripts/gen-fixture.ts, apps/api/scripts/memtest.ts, apps/api/scripts/memtest-sweep.ts, apps/api/scripts/memory-process-contract.test.mjs, apps/api/package.json, .github/workflows/memory.yml]
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-04]
must_haves:
  truths:
    - "The authoritative fixture is at least 512 MiB by exact byte assertion and therefore strictly greater than 500 MiB."
    - "The self-test drains ReportParser without collecting results, samples rss/heapUsed/external, and fails on parser, OOM, or threshold errors."
    - "The default sweep covers 50/200/500MB and the 1GB case is explicit opt-in with cleanup and a documented RSS band."
    - "Node 22 CI creates the authoritative fixture immediately before invoking the exact 150MB-heap command and fails closed."
  artifacts:
    - path: apps/api/scripts/gen-fixture.ts
      provides: Backpressure-aware deterministic Trivy report generator
    - path: apps/api/scripts/memtest.ts
      provides: Peak memory self-test and exit gate
    - path: apps/api/scripts/memtest-sweep.ts
      provides: Flat-RSS default matrix and 1GB opt-in
    - path: apps/api/scripts/memory-process-contract.test.mjs
      provides: Static/unit process-safety assertions
    - path: .github/workflows/memory.yml
      provides: Node 22 CI memory gate
  key_links:
    - from: gen-fixture.ts
      to: memtest.ts
      via: Generated fixture path
    - from: memtest.ts
      to: ReportParser
      via: Direct async-generator drain
    - from: memory.yml
      to: generator and memtest
      via: Ordered Node 22 command sequence
---

<objective>
Prove the parser's memory behavior under the assignment heap cap, exercise flat RSS across increasing files, and make the strictly-over-500MB claim a reproducible Node 22 CI gate.

Purpose: Turn MEM-01 through MEM-04 into executable evidence while keeping tooling isolated from NestJS, queue, HTTP, Docker, and Trivy runtime plumbing.
Output: Bounded generator, memory self-test, sweep, process-safety contract, package scripts, and fail-closed workflow.
</objective>

<execution_context>
@/home/dzmitry/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/dzmitry/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md
@.planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md
@.planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md
@.planning/phases/02-streaming-parse-pipeline-memory-proof/02-01-PLAN.md
@apps/api/package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build and verify the bounded fixture generator</name>
  <files>apps/api/scripts/gen-fixture.ts, apps/api/package.json</files>
  <read_first>apps/api/scripts/gen-fixture.ts; apps/api/package.json; apps/api/src/worker.ts; apps/api/src/index.ts; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md</read_first>
  <acceptance_criteria>Typecheck passes; the 1MB generator output meets its byte floor and contains CRITICAL and HIGH; the 512MiB output is at least 512*1024*1024 bytes and greater than 500*1024*1024; invalid size/path input exits before creating output; source shows backpressure drain handling, no report-sized array, and a non-zero top-level failure path.</acceptance_criteria>
  <action>Per D-03, create gen-fixture.ts with validated size, output path, and deterministic severity/shape options. Follow the CLI failure-boundary/error-handling analog in apps/api/src/worker.ts identified by 02-PATTERNS.md: use a top-level async boundary, report errors, and exit non-zero rather than silently succeeding. Stream the concentrated single-Result Trivy JSON using createWriteStream, one bounded vulnerability JSON object at a time, explicit drain waits, and finish handling; retain no array or whole document. Make `--size-mb 512` target at least `512 * 1024 * 1024` bytes and report final bytes/counts, so the authoritative artifact is strictly above `500 * 1024 * 1024`. Reject invalid size/path arguments before opening output. Add gen:fixture without runtime wiring.</action>
  <verify><automated>npm run typecheck --workspace apps/api && npm run gen:fixture --workspace apps/api -- --size-mb 1 --output /tmp/oxtest-phase2-fixture.json && node -e "const fs=require('fs'); const p='/tmp/oxtest-phase2-fixture.json'; const n=fs.statSync(p).size; if(n<1*1024*1024) process.exit(1); const t=fs.readFileSync(p,'utf8'); if(!t.includes('CRITICAL')||!t.includes('HIGH')) process.exit(1)"</automated>
    <automated>npm run gen:fixture --workspace apps/api -- --size-mb 512 --output /tmp/oxtest-phase2-memory.json && node -e "const n=require('fs').statSync('/tmp/oxtest-phase2-memory.json').size; if(n < 512*1024*1024 || n <= 500*1024*1024) process.exit(1)"</automated></verify>
  <done>Generator streams with backpressure, rejects invalid inputs before file creation, and passes exact-byte small and authoritative-size assertions.</done>
</task>
<task type="auto">
  <name>Task 2: Build memory self-test and sweep harnesses</name>
  <files>apps/api/scripts/memtest.ts, apps/api/scripts/memtest-sweep.ts, apps/api/scripts/memory-process-contract.test.mjs, apps/api/package.json</files>
  <read_first>apps/api/scripts/memtest.ts; apps/api/scripts/memtest-sweep.ts; apps/api/scripts/memory-process-contract.test.mjs; apps/api/package.json; apps/api/src/worker.ts; apps/api/src/index.ts; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-01-PLAN.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md</read_first>
  <acceptance_criteria>Typecheck, memory-process contract, build, default sweep, and both dry-run modes pass; default cases are exactly 50/200/500MB and 1GB runs only with --include-1gb; memtest discards yields, samples all three metrics, clears its sampler in finally, and exits non-zero for usage/parser/OOM/threshold failures; contract assertions prove argv-array construction, shell:false, validation before spawn, no shell wrapper, and cleanup.</acceptance_criteria>
  <action>Per D-05, create memtest.ts requiring a fixture path, sampling rss/heapUsed/external before parsing, draining and discarding every ReportParser yield, clearing the sampler in finally, logging structured metrics, and exiting non-zero for usage, parser, OOM, or RSS-threshold failure. Follow the explicit async main/catch and non-zero failure-boundary pattern in apps/api/src/worker.ts, as assigned in 02-PATTERNS.md, for CLI error handling. Per D-06, create memtest-sweep.ts with sequential 50/200/500MB defaults, explicit --include-1gb opt-in, isolated paths, direct child-process calls using an argv array and shell false, documented constant RSS band plus Linux allocator margin, and per-case cleanup. Add dry-run proof for 1GB. Validate every child argument before spawn and reject invalid values before invocation; never use shell wrappers or concatenated command strings. Add a node:test contract that mechanically asserts argv is an array, shell is false, no shell-wrapper/concatenated command construction exists, and validation precedes spawn; expose a package command for it.</action>
  <verify><automated>npm run typecheck --workspace apps/api && npm run test:memory-contract --workspace apps/api</automated>
    <automated>npm run build --workspace apps/api && npm run memtest:sweep --workspace apps/api -- --dry-run && npm run memtest:sweep --workspace apps/api -- --include-1gb --dry-run</automated>
    <automated>npm run memtest:sweep --workspace apps/api && node -e "const fs=require('fs'); const g=fs.readFileSync('apps/api/scripts/gen-fixture.ts','utf8'); const m=fs.readFileSync('apps/api/scripts/memtest.ts','utf8'); const s=fs.readFileSync('apps/api/scripts/memtest-sweep.ts','utf8'); if(!g.includes('drain')||!m.includes('process.exit(1)')||!m.includes('finally')||!s.includes('finally')) process.exit(1)"</automated></verify>
  <done>Self-test, sweep, and source contract prove generator backpressure, memtest failure behavior, default bands, cleanup, 1GB opt-in, and safe child-process construction.</done>
</task>
<task type="auto">
  <name>Task 3: Add and execute the authoritative Node 22 CI proof</name>
  <files>.github/workflows/memory.yml, apps/api/scripts/gen-fixture.ts, apps/api/scripts/memtest.ts, apps/api/scripts/memtest-sweep.ts, apps/api/package.json</files>
  <read_first>.github/workflows/memory.yml; apps/api/scripts/gen-fixture.ts; apps/api/scripts/memtest.ts; apps/api/scripts/memtest-sweep.ts; apps/api/package.json; apps/api/src/worker.ts; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-01-PLAN.md</read_first>
  <acceptance_criteria>Workflow text contains Node 22 setup, lockfile install, fail-closed sequencing, immediate 512MiB generation, the exact stat byte assertion, and the exact `node --max-old-space-size=150 apps/api/dist/scripts/memtest.js /tmp/oxtest-phase2-memory.json` command; it excludes --include-1gb; local focused checks, default sweep, and opt-in dry-run pass; git diff --check passes.</acceptance_criteria>
  <action>Per D-09, create a Node 22 checkout/setup workflow with lockfile install, build, metrics output, and cleanup that cannot mask failure. Follow the CLI failure-boundary/error-handling convention in apps/api/src/worker.ts from 02-PATTERNS.md for fail-closed process behavior, adapting it to the workflow's ordered shell commands. Its ordered authoritative sequence must first exit non-zero unless `node` major version is 22, then execute `npm run build --workspace apps/api`, generate `/tmp/oxtest-phase2-memory.json` immediately before use with `npm run gen:fixture --workspace apps/api -- --size-mb 512 --output /tmp/oxtest-phase2-memory.json`, assert `test "$(stat -c %s /tmp/oxtest-phase2-memory.json)" -ge "$((512*1024*1024))"`, and invoke exactly `node --max-old-space-size=150 apps/api/dist/scripts/memtest.js /tmp/oxtest-phase2-memory.json`. Do not include the 1GB opt-in in CI. Run focused parser test, typecheck, lint, process contract, default sweep, and opt-in dry-run locally; the exact self-test is authoritative only when the executable Node 22 condition passes. Audit that no later-phase runtime subsystem or accumulation was added.</action>
  <verify><automated>npm test --workspace apps/api -- --runInBand src/parser/report-parser.spec.ts && npm run typecheck --workspace apps/api && npm run lint --workspace apps/api && npm run test:memory-contract --workspace apps/api && npm run memtest:sweep --workspace apps/api</automated>
    <automated>if [ "$(node -p "process.versions.node.split('.')[0]")" != "22" ]; then printf '%s\n' 'Node 22 required for authoritative self-test' >&2; exit 2; fi; npm run build --workspace apps/api && npm run gen:fixture --workspace apps/api -- --size-mb 512 --output /tmp/oxtest-phase2-memory.json && test "$(stat -c %s /tmp/oxtest-phase2-memory.json)" -ge "$((512*1024*1024))" && node --max-old-space-size=150 apps/api/dist/scripts/memtest.js /tmp/oxtest-phase2-memory.json</automated>
    <automated>npm run memtest:sweep --workspace apps/api -- --include-1gb --dry-run && node -e "const fs=require('fs'); const w=fs.readFileSync('.github/workflows/memory.yml','utf8'); if(w.includes('include-1gb')||!w.includes('node-version: 22')||!w.includes('--size-mb 512')||!w.includes('--max-old-space-size=150 apps/api/dist/scripts/memtest.js /tmp/oxtest-phase2-memory.json')) process.exit(1)"</automated>
    <automated>git diff --check</automated></verify>
  <done>CI has an executable Node 22 condition, creates and byte-asserts the 512 MiB fixture immediately before the exact self-test, fails closed, and preserves the separate 50/200/500MB matrix plus explicit 1GB coverage.</done>
</task>
</tasks>

<source_audit>
| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL | CRITICAL-only streaming parse under 150MB before queue/HTTP | 02-01, 02-02 | COVERED |
| REQ | ENGINE-05 | 02-01 | COVERED |
| REQ | MEM-01, MEM-02, MEM-03, MEM-04 | 02-01, 02-02 | COVERED |
| RESEARCH | Deep leaf Pick + streamValues, CJS pins, backpressure, memory sampling, skewed fixture, static guard | 02-01, 02-02 | COVERED |
| CONTEXT | D-01 through D-04 | 02-01 | COVERED |
| CONTEXT | D-05 through D-06 | 02-02 | COVERED |
| CONTEXT | D-07 through D-08 | 02-01 | COVERED |
| CONTEXT | D-09 | 02-02 | COVERED |
</source_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CLI → generator | Size/path values affect resources and filesystem. |
| Generated report → parser | Synthetic JSON exercises parse and memory limits. |
| Workflow → runner | CI controls processes, disk, and cleanup. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Tampering | Generator CLI | high | mitigate | Validate finite bounded size/path before opening files. |
| T-02-06 | Denial of Service | Generator/self-test | high | mitigate | Backpressure, discard yields, sampling, RSS gate, and OOM failure. |
| T-02-07 | Tampering | Child process construction | high | mitigate | Validated argv arrays and shell false, enforced by source contract. |
| T-02-08 | Denial of Service | CI artifacts | medium | mitigate | Ephemeral fixture, final cleanup, no default 1GB run, fail-closed commands. |
| T-02-09 | Elevation of Privilege | CI dependencies | medium | mitigate | Pinned action majors, Node 22, lockfile install. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Plan 01 legitimacy checkpoint and exact lockfile pins. |
</threat_model>

<verification>Run focused Jest/typecheck/lint, generator byte assertion, process contract, default sweep, 1GB dry-run, and the ordered Node 22 authoritative command. Confirm workflow cleanup cannot override the main result.</verification>
<success_criteria>The parser passes a strictly-over-500MiB fixture under the 150MB V8 heap in Node 22 CI; default and opt-in sweeps prove flat RSS; generator backpressure and subprocess safety are mechanically tested; and no later-phase plumbing exists.</success_criteria>
<output>Create .planning/phases/02-streaming-parse-pipeline-memory-proof/02-02-SUMMARY.md when done.</output>
