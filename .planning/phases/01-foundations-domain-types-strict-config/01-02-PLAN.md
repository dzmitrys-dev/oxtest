---
phase: 01-foundations-domain-types-strict-config
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/api/src/index.ts
  - apps/api/src/worker.ts
  - apps/api/src/app.module.ts
  - apps/api/src/worker.module.ts
  - apps/api/src/scan/scan.module.ts
  - apps/api/src/scan/scan.store.ts
  - apps/api/src/config/env.validation.ts
  - apps/api/src/domain/scan.types.ts
  - apps/api/src/domain/vulnerability.types.ts
  - apps/api/src/domain/trivy-report.types.ts
  - apps/api/.env.example
  - apps/api/src/main.ts
  - apps/api/src/app.controller.ts
  - apps/api/src/app.service.ts
  - apps/api/src/app.controller.spec.ts
autonomous: true
requirements:
  - TYPE-02
  - ARCH-04
  - OPS-03
user_setup: []

must_haves:
  truths:
    - "`node apps/api/dist/index.js` (valid env) binds an HTTP listener on PORT; an HTTP request to it gets a response (404 on `/` proves the listener)"
    - "`node apps/api/dist/worker.js` (valid env) boots an application context and binds NO HTTP port"
    - "Booting either entrypoint with REDIS_HOST unset (and no .env supplying it) exits non-zero with a Joi message naming REDIS_HOST"
    - "Booting with valid config starts cleanly (exit stays 0 / process stays alive)"
    - "Scan, Vulnerability, ScanStatus (enum) and the Trivy report shape exist as explicit framework-free TypeScript interfaces/enums"
    - "Domain types (Scan/ScanStatus) are consumed OUTSIDE apps/api/src/domain/ — the ScanStore stub in ScanModule imports them and ScanModule exports it, so both AppModule and WorkerModule (API + worker tiers) inherit the provider (Success Criterion #4: types used across layers, not merely defined)"
    - "`tsc --noEmit` still exits 0 after all new code"
  artifacts:
    - "apps/api/src/index.ts (API entrypoint -> dist/index.js)"
    - "apps/api/src/worker.ts (worker entrypoint -> dist/worker.js)"
    - "apps/api/src/app.module.ts, apps/api/src/worker.module.ts, apps/api/src/scan/scan.module.ts"
    - "apps/api/src/config/env.validation.ts (Joi schema)"
    - "apps/api/src/domain/{scan,vulnerability,trivy-report}.types.ts"
    - "apps/api/.env.example"
  key_links:
    - "AppModule imports ConfigModule.forRoot({ isGlobal, validationSchema }) + ScanModule (D-06)"
    - "WorkerModule imports ConfigModule + ScanModule but NEVER any HTTP/GraphQL module (D-06 dead-heap + dead-attack-surface guard)"
    - "Both entrypoints wrap bootstrap().catch(err => { console.error(err); process.exit(1); }) for a deterministic non-zero exit on invalid config (Pitfall 5)"
    - "Both entrypoints import the SAME ScanModule via their root module (the shared DI seam every later phase plugs into)"
---

<objective>
Build the framework-free domain type layer, the two-entrypoint module topology (shared `ScanModule`), and boot-time fail-fast Joi env validation on top of the strict scaffold from Plan 01.

Purpose: This delivers three of the phase's four requirements — TYPE-02 (explicit domain + Trivy interfaces), ARCH-04 (API entrypoint with an HTTP listener + worker entrypoint with none, both sharing `ScanModule`), and OPS-03 (the app refuses to boot on missing/invalid config with a clear Joi message). It establishes the `ScanModule` seam that Phases 2-6 extend.
Output: `dist/index.js` boots an HTTP API; `dist/worker.js` boots a listener-less worker context; both fail fast and non-zero on invalid env; domain models exist as strict interfaces.
</objective>

<execution_context>
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/workflows/execute-plan.md
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-domain-types-strict-config/01-CONTEXT.md
@.planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundations-domain-types-strict-config/01-01-SUMMARY.md
</context>

<artifacts_produced>
Symbols / files this plan creates (source-grounding reference — these are net-new, not drift):
- Enums: `ScanStatus` (Queued | Scanning | Finished | Failed) — `apps/api/src/domain/scan.types.ts`
- Interfaces: `Scan` (`scan.types.ts`); `Vulnerability` (`vulnerability.types.ts`); `TrivyReport`, `TrivyResult`, `TrivyVulnerability` (`trivy-report.types.ts`)
- Consts: `envValidationSchema` (Joi.object) — `apps/api/src/config/env.validation.ts`
- Classes/modules: `AppModule` (`app.module.ts`), `WorkerModule` (`worker.module.ts`), `ScanModule` (`scan/scan.module.ts` — provides + exports the `ScanStore` stub), `ScanStore` (`scan/scan.store.ts` — `@Injectable()` typed stub over an in-memory `Map<string, Scan>`, methods reference `Scan`/`ScanStatus`, no business logic)
- Entrypoint files: `src/index.ts` (API), `src/worker.ts` (worker)
- Config artifact: `apps/api/.env.example`
- Removed this plan: `src/main.ts`, `src/app.controller.ts`, `src/app.service.ts`, `src/app.controller.spec.ts` (scaffold leftovers superseded by the two-entrypoint layout)
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Framework-free domain type layer (TYPE-02)</name>
  <files>apps/api/src/domain/scan.types.ts, apps/api/src/domain/vulnerability.types.ts, apps/api/src/domain/trivy-report.types.ts</files>
  <read_first>
    - .planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md — "Code Examples > Domain types skeleton" (exact interface/enum shapes)
    - .planning/phases/01-foundations-domain-types-strict-config/01-CONTEXT.md — D-03 (plain framework-free TS interfaces/enums; GraphQL @ObjectType maps to these later — no decorators in domain), D-04 (minimal Trivy shape — only the path actually parsed)
    - .planning/research/STACK.md — "Trivy CLI Reference" JSON report shape (Results[].Vulnerabilities[])
  </read_first>
  <action>
    Create three framework-free files under `apps/api/src/domain/` — plain TypeScript, ZERO runtime dependencies, NO NestJS/GraphQL decorators or imports (D-03: the domain stays pure so the worker never pulls GraphQL decorator metadata).

    `scan.types.ts`: export enum `ScanStatus` with string members `Queued = 'Queued'`, `Scanning = 'Scanning'`, `Finished = 'Finished'`, `Failed = 'Failed'`. Export interface `Scan` with: `id: string`, `status: ScanStatus`, `repoUrl: string`, optional `vulnerabilities?: Vulnerability[]` (import the type from `./vulnerability.types`), optional `error?: string`, `createdAt: string`, `updatedAt: string`. Use `import type` for the Vulnerability type if importing only its type.

    `vulnerability.types.ts`: export interface `Vulnerability` with the CRITICAL-relevant fields (camelCase domain naming): `vulnerabilityId: string`, `pkgName: string`, `installedVersion: string`, `severity: 'CRITICAL'`, `title: string`, `primaryUrl: string`. Per D-04 the domain-side severity is narrowed to the literal `'CRITICAL'` because only CRITICAL vulns are ever stored.

    `trivy-report.types.ts`: model ONLY the parse path (D-04 — do not model Trivy's full schema). Export interface `TrivyReport` with optional `Results?: TrivyResult[]`. Export interface `TrivyResult` with `Target: string` and optional `Vulnerabilities?: TrivyVulnerability[]`. Export interface `TrivyVulnerability` with the raw Trivy field names (PascalCase, matching the JSON): `VulnerabilityID: string`, `PkgName: string`, `InstalledVersion: string`, `Severity: 'CRITICAL' | 'HIGH' | 'MEDIUM' | 'LOW' | 'UNKNOWN'`, `Title: string`, `PrimaryURL: string`.

    These are the source-of-truth types Phases 2-4 consume (parser output, repository records, GraphQL object types map onto them). Do not add index barrels unless needed for a clean import.
  </action>
  <verify>
    <automated>cd /home/dzmitry/Coding/oxtest && test -f apps/api/src/domain/scan.types.ts && test -f apps/api/src/domain/vulnerability.types.ts && test -f apps/api/src/domain/trivy-report.types.ts && grep -q 'enum ScanStatus' apps/api/src/domain/scan.types.ts && grep -qE 'interface (Scan|Vulnerability|TrivyReport|TrivyResult|TrivyVulnerability)' apps/api/src/domain/scan.types.ts apps/api/src/domain/vulnerability.types.ts apps/api/src/domain/trivy-report.types.ts && ! grep -rn '@nestjs' apps/api/src/domain && npm run typecheck --workspace apps/api && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist under `apps/api/src/domain/`.
    - `ScanStatus` is an enum with exactly Queued/Scanning/Finished/Failed string members.
    - `Scan`, `Vulnerability`, `TrivyReport`, `TrivyResult`, `TrivyVulnerability` are exported interfaces.
    - No `@nestjs/*` or GraphQL import appears anywhere under `apps/api/src/domain/` (D-03 purity).
    - `npm run typecheck --workspace apps/api` still exits 0.
  </acceptance_criteria>
  <done>The domain layer expresses Scan, Vulnerability, ScanStatus, and the minimal Trivy report shape as explicit, decorator-free TypeScript interfaces/enums — satisfying TYPE-02 and D-03/D-04.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Joi env schema + shared ScanModule + AppModule/WorkerModule topology (OPS-03, ARCH-04)</name>
  <files>apps/api/src/config/env.validation.ts, apps/api/src/scan/scan.module.ts, apps/api/src/scan/scan.store.ts, apps/api/src/app.module.ts, apps/api/src/worker.module.ts, apps/api/.env.example</files>
  <read_first>
    - .planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md — "Pattern 2: Boot-time fail-fast env validation" (ConfigModule.forRoot({validationSchema}) wiring + failure semantics), "Open Question 2" (full env schema now with safe .env.example defaults), Security Domain (V14 fail-closed config)
    - .planning/phases/01-foundations-domain-types-strict-config/01-CONTEXT.md — D-06 (shared ScanModule; AppModule vs WorkerModule; worker imports NO GraphQL/HTTP), Claude's Discretion (env var set: REDIS_HOST/REDIS_PORT/PORT/temp dir/Trivy-mode; directory naming)
    - .planning/research/ARCHITECTURE.md — "Architectural Responsibility Map" (domain types must be imported by BOTH API and worker tiers)
    - apps/api/src/domain/scan.types.ts — the `Scan` interface + `ScanStatus` enum the ScanStore stub references (created in Task 1)
    - apps/api/src/app.module.ts — the CLI-generated AppModule to be overwritten
  </read_first>
  <action>
    Create `apps/api/src/config/env.validation.ts` exporting `envValidationSchema = Joi.object({...})` (import Joi as `import * as Joi from 'joi'`). Define the full downstream env surface now (Open Question 2 — proves the fail-fast mechanism against the real eventual surface, avoids a schema migration later):
      - `NODE_ENV`: string valid 'development'|'production'|'test', default 'development'
      - `PORT`: number `.port()`, default 3000
      - `REDIS_HOST`: string `.required()`
      - `REDIS_PORT`: number `.port().required()`
      - `SCAN_TMP_DIR`: string `.required()`
      - `TRIVY_MODE`: string valid 'binary'|'docker', default 'binary'
    Connectivity-critical keys use `.required()` with NO default (Security V14 fail-closed: no silent unsafe default — threat T-01-02).

    Create `apps/api/src/scan/scan.store.ts`: an `@Injectable()` class `ScanStore` that holds `private readonly scans = new Map<string, Scan>()` (import `Scan` and `ScanStatus` from `../domain/scan.types`). Give it typed stub methods that REFERENCE the domain types (this is the concrete cross-layer usage point that satisfies Success Criterion #4 — no business logic, in-memory only): `get(id: string): Scan | null` returning `this.scans.get(id) ?? null` (the `?? null` keeps it strict under noUncheckedIndexedAccess); `list(): Scan[]` returning `[...this.scans.values()]`; `listByStatus(status: ScanStatus): Scan[]` returning `this.list().filter((s) => s.status === status)`. Real persistence (Redis `ScanRepository`) is Phase 3 — do NOT add it here.

    Create `apps/api/src/scan/scan.module.ts`: `@Module({ providers: [ScanStore], exports: [ScanStore] })` on the `ScanModule` class. Exporting `ScanStore` means both `AppModule` and `WorkerModule` (which each import `ScanModule`) inherit the provider in their DI graph — so the domain types are wired into BOTH the API and worker tiers (RESEARCH.md Architectural Responsibility Map). Do NOT import HTTP or GraphQL modules into ScanModule.

    Overwrite `apps/api/src/app.module.ts`: `AppModule` imports `ConfigModule.forRoot({ isGlobal: true, validationSchema: envValidationSchema })` and `ScanModule`. Do not add controllers/providers (the generated AppController/AppService are removed in Task 3). This is the API-side root module; HTTP transport/GraphQL are added in Phase 4 — none here yet.

    Create `apps/api/src/worker.module.ts`: `WorkerModule` imports the SAME `ConfigModule.forRoot({ isGlobal: true, validationSchema: envValidationSchema })` and `ScanModule` — and NOTHING HTTP/GraphQL (D-06: dead-heap AND dead-attack-surface guard; a standalone application context has no HTTP request lifecycle, so any HTTP/GraphQL module there would be inert AND wasteful). Both root modules therefore share ScanModule and the identical validation schema, so both entrypoints fail-fast identically.

    Create `apps/api/.env.example` with safe local-dev defaults so docker-compose/local dev is unblocked before Phase 3: `NODE_ENV=development`, `PORT=3000`, `REDIS_HOST=localhost`, `REDIS_PORT=6379`, `SCAN_TMP_DIR=/tmp/scans`, `TRIVY_MODE=binary`. Do NOT create a real `.env` (only the example is committed).
  </action>
  <verify>
    <automated>cd /home/dzmitry/Coding/oxtest && grep -q "envValidationSchema" apps/api/src/config/env.validation.ts && grep -qE "REDIS_HOST.*required" apps/api/src/config/env.validation.ts && grep -q "validationSchema" apps/api/src/app.module.ts && grep -q "validationSchema" apps/api/src/worker.module.ts && ! grep -rnE '@nestjs/(platform-fastify|graphql|mercurius|apollo)' apps/api/src/worker.module.ts && grep -q "ScanModule" apps/api/src/app.module.ts && grep -q "ScanModule" apps/api/src/worker.module.ts && grep -q "ScanStore" apps/api/src/scan/scan.module.ts && grep -q "exports" apps/api/src/scan/scan.module.ts && grep -rE "domain/(scan|vulnerability|trivy-report)" apps/api/src --include=*.ts | grep -v "/domain/" | grep -q . && test -f apps/api/.env.example && npm run typecheck --workspace apps/api && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `env.validation.ts` exports `envValidationSchema` (Joi.object) with `REDIS_HOST`, `REDIS_PORT`, `SCAN_TMP_DIR` marked `.required()` (no default) and `NODE_ENV`/`PORT`/`TRIVY_MODE` with defaults.
    - `AppModule` and `WorkerModule` both wire `ConfigModule.forRoot({ isGlobal: true, validationSchema: envValidationSchema })` and both import `ScanModule`.
    - `ScanModule` provides AND exports `ScanStore`; `scan.store.ts` imports `Scan`/`ScanStatus` from `../domain/scan.types`, so at least one import of a `domain/{scan,vulnerability,trivy-report}` type exists in a file OUTSIDE `apps/api/src/domain/` (cross-layer usage — Success Criterion #4; `grep -rE "domain/(scan|vulnerability|trivy-report)" apps/api/src --include=*.ts | grep -v "/domain/"` returns >=1 match).
    - `worker.module.ts` contains no `@nestjs/platform-fastify`, `@nestjs/graphql`, `@nestjs/mercurius`, or Apollo import (D-06 guard).
    - `apps/api/.env.example` exists with all six keys and safe defaults; no committed `.env`.
    - `npm run typecheck --workspace apps/api` exits 0.
  </acceptance_criteria>
  <done>A single Joi schema validates the full env surface fail-closed; ScanModule is the shared seam that provides+exports the typed `ScanStore` stub (referencing `Scan`/`ScanStatus`); AppModule and WorkerModule import it plus the identical ConfigModule, so domain types are consumed across both tiers, with the worker carrying zero HTTP/GraphQL surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Two entrypoints (index.ts API + worker.ts) with fail-fast boot, verified (ARCH-04, OPS-03)</name>
  <files>apps/api/src/index.ts, apps/api/src/worker.ts, apps/api/src/main.ts, apps/api/src/app.controller.ts, apps/api/src/app.service.ts, apps/api/src/app.controller.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md — "Code Examples > Full env-validated, dual-entrypoint bootstrap" (exact index.ts/worker.ts), "Pattern 1" (createApplicationContext has no network listeners), Pitfall 5 (explicit .catch + process.exit(1) for deterministic non-zero exit), Pitfall 3 (two entrypoints, not nest start)
    - .planning/phases/01-foundations-domain-types-strict-config/01-CONTEXT.md — D-02 (index.ts basename -> dist/index.js), D-06 (NestFactory.create(AppModule, FastifyAdapter)+listen vs createApplicationContext(WorkerModule), no listener), <specifics> (worker excludes HTTP/GraphQL deliberately)
    - apps/api/src/main.ts — the generated entrypoint to be deleted/replaced
  </read_first>
  <action>
    Create `apps/api/src/index.ts` (API entrypoint, D-02 basename): an async `bootstrap()` that calls `NestFactory.create<NestFastifyApplication>(AppModule, new FastifyAdapter())` (import `FastifyAdapter, NestFastifyApplication` from `@nestjs/platform-fastify`), then `app.enableShutdownHooks()`, then `await app.listen(process.env.PORT ?? 3000, '0.0.0.0')`. Invoke it as `bootstrap().catch((err) => { console.error(err); process.exit(1); })` (Pitfall 5 — guarantees a printed Joi message AND a deterministic non-zero exit on invalid config; do not rely on Node's default unhandled-rejection behavior). Emit a concise ready log after listen so the boot check can confirm start.

    Create `apps/api/src/worker.ts` (worker entrypoint): an async `bootstrap()` that calls `NestFactory.createApplicationContext(WorkerModule)` then `app.enableShutdownHooks()` and NOTHING ELSE — no `app.listen()`, no `app.close()` (Pattern 1: the standalone context has no network listener; it stays alive until SIGTERM/SIGINT once BullMQ is wired in Phase 3). Log a ready line such as "Worker application context started" so the boot check can confirm it started with no port bound. Invoke with the identical `bootstrap().catch((err) => { console.error(err); process.exit(1); })` pattern.

    Delete the scaffold leftovers superseded by the two-entrypoint layout: `apps/api/src/main.ts`, `apps/api/src/app.controller.ts`, `apps/api/src/app.service.ts`, `apps/api/src/app.controller.spec.ts`. (The skeleton has no HTTP controllers yet; AppModule already dropped them in Task 2.)

    Build with `npm run build --workspace apps/api` and confirm BOTH `apps/api/dist/index.js` and `apps/api/dist/worker.js` are emitted (Pitfall 3 — `nest build` compiles every top-level src file, not just one `entryFile`).

    Note for SUMMARY: the assignment self-test command becomes `node --max-old-space-size=150 apps/api/dist/index.js` (D-02 path shift) — this must be surfaced in README in Phase 6 (DOC-01).
  </action>
  <verify>
    <automated>cd /home/dzmitry/Coding/oxtest && npm run build --workspace apps/api && test -f apps/api/dist/index.js && test -f apps/api/dist/worker.js && ! test -f apps/api/src/main.ts && grep -q 'createApplicationContext' apps/api/src/worker.ts && ! grep -q '\.listen(' apps/api/src/worker.ts && grep -q 'FastifyAdapter' apps/api/src/index.ts && grep -q 'process.exit(1)' apps/api/src/index.ts && grep -q 'process.exit(1)' apps/api/src/worker.ts && echo BUILD_OK
# Fail-fast: worker with required env removed and no .env in cwd must exit non-zero and name REDIS_HOST
cd /home/dzmitry/Coding/oxtest/apps/api && OUT=$(env -u REDIS_HOST -u REDIS_PORT -u SCAN_TMP_DIR node dist/worker.js 2>&1); RC=$?; if [ $RC -eq 0 ]; then echo "FAIL: booted despite missing config"; exit 1; fi; echo "$OUT" | grep -q 'REDIS_HOST' && echo FAILFAST_OK
# Positive worker boot: poll (max ~20s) for ready log, assert NO port bound, then terminate
cd /home/dzmitry/Coding/oxtest/apps/api && REDIS_HOST=localhost REDIS_PORT=6379 SCAN_TMP_DIR=/tmp/scans node dist/worker.js > /tmp/w.log 2>&1 & WPID=$!
READY=0; for i in $(seq 1 40); do if grep -qi 'worker' /tmp/w.log 2>/dev/null; then READY=1; break; fi; kill -0 $WPID 2>/dev/null || break; sleep 0.5; done
PORT_OPEN=0; if command -v nc >/dev/null 2>&1 && nc -z localhost 3000 2>/dev/null; then PORT_OPEN=1; fi
kill $WPID 2>/dev/null; wait $WPID 2>/dev/null
if [ "$READY" = "1" ] && [ "$PORT_OPEN" = "0" ]; then echo WORKER_BOOT_OK; else echo "FAIL: worker not ready ($READY) or bound a port ($PORT_OPEN)"; cat /tmp/w.log; exit 1; fi
# Positive API boot: poll (max ~20s) until HTTP listener answers (any status, e.g. 404, proves listener), then terminate
cd /home/dzmitry/Coding/oxtest/apps/api && REDIS_HOST=localhost REDIS_PORT=6379 SCAN_TMP_DIR=/tmp/scans PORT=3000 node dist/index.js > /tmp/a.log 2>&1 & APID=$!
CODE=000; for i in $(seq 1 40); do CODE=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/ 2>/dev/null || echo 000); [ "$CODE" != "000" ] && break; kill -0 $APID 2>/dev/null || break; sleep 0.5; done
kill $APID 2>/dev/null; wait $APID 2>/dev/null
if [ "$CODE" != "000" ]; then echo "API_BOOT_OK (HTTP $CODE)"; else echo "FAIL: API never bound a listener"; cat /tmp/a.log; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build --workspace apps/api` emits BOTH `apps/api/dist/index.js` AND `apps/api/dist/worker.js`.
    - `apps/api/src/index.ts` constructs the app via `NestFactory.create<NestFastifyApplication>(AppModule, new FastifyAdapter())` and calls `app.listen(...)`.
    - `apps/api/src/worker.ts` uses `NestFactory.createApplicationContext(WorkerModule)` and contains NO `.listen(` call.
    - Both entrypoints invoke `bootstrap().catch(...)` that calls `process.exit(1)`.
    - `apps/api/src/main.ts` (and generated app.controller/app.service/spec) no longer exist.
    - Running `node dist/worker.js` with REDIS_HOST/REDIS_PORT/SCAN_TMP_DIR unset (no `.env` present) exits non-zero and stderr contains "REDIS_HOST".
    - Running `node dist/worker.js` with valid env boots (logs a worker-ready line) and binds no HTTP port.
    - Running `node dist/index.js` with valid env answers an HTTP request on PORT (any HTTP status code, e.g. 404, proves the listener is up).
  </acceptance_criteria>
  <done>Two entrypoints exist: `dist/index.js` boots a Fastify HTTP API and `dist/worker.js` boots a listener-less application context, both importing the shared ScanModule; both fail fast with a non-zero exit and a clear Joi message on invalid config — satisfying ARCH-04 and the boot-behavior half of OPS-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env -> application config | Untrusted/absent env values cross into the app at boot |
| error/stderr output -> operator logs | Bootstrap failures print to stderr where an operator reads them |
| API process HTTP listener -> network | `dist/index.js` binds a port (the worker deliberately does not) |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering / Denial of Service | boot-time config (env.validation.ts) | high | mitigate | Joi `validationSchema` marks REDIS_HOST/REDIS_PORT/SCAN_TMP_DIR `.required()` with no default (fail-closed, ASVS V14.1); Nest throws synchronously during bootstrap; both entrypoints wrap `bootstrap().catch` with `process.exit(1)` for a deterministic non-zero exit (Pitfall 5) |
| T-01-03 | Information Disclosure | Joi validation error / stderr | low | mitigate | No secret-shaped env vars defined this phase; Joi default `.required()` errors name the KEY, not the value; flag for Phase 3/4 planner: when Redis-auth / secret keys are added, verify error logging does not echo raw values |
| T-01-04 | Elevation of Privilege / Info Disclosure | worker process attack surface | low | mitigate | WorkerModule imports ScanModule + ConfigModule only; `createApplicationContext` binds no network listener, so the worker exposes zero HTTP/GraphQL attack surface (D-06 dead-surface guard) |
| T-01-DEF | Injection (command) | user repo URL -> git/Trivy exec | n/a this phase | transfer (Phase 3) | DEFERRED: no exec/clone/subprocess code is written in Phase 1. The command-injection surface (argv-array exec, URL allowlisting) is modeled and mitigated in Phase 3 (ENGINE-02, SCAN-02). Recorded here only to mark it explicitly out-of-scope-this-phase, not overlooked. |
</threat_model>

<verification>
- `npm run typecheck --workspace apps/api` exits 0 with all new domain/module/entrypoint code.
- `npm run build --workspace apps/api` emits both `dist/index.js` and `dist/worker.js`.
- Domain interfaces (Scan, Vulnerability, ScanStatus, TrivyReport/TrivyResult/TrivyVulnerability) exist and are decorator-free.
- `dist/index.js` binds an HTTP listener on PORT (HTTP request returns a status code); `dist/worker.js` binds no port.
- Missing REDIS_HOST -> non-zero exit + Joi message naming REDIS_HOST; valid env -> clean boot.
</verification>

<success_criteria>
Satisfies Phase 1 Success Criteria #2 (ARCH-04: two entrypoints sharing ScanModule — API with HTTP listener, worker without), #3 (OPS-03: fail-fast Joi validation, non-zero exit on invalid config, clean boot on valid config), and #4 (TYPE-02: Scan/Vulnerability/ScanStatus + Trivy report shape as explicit interfaces).
</success_criteria>

<output>
Create `.planning/phases/01-foundations-domain-types-strict-config/01-02-SUMMARY.md` when done. Record: the final env-var schema (keys + which are required), confirmation both dist entrypoints boot and the worker binds no port, the fail-fast test result (exit code + that the Joi message named REDIS_HOST), and a reminder that the self-test path is `node --max-old-space-size=150 apps/api/dist/index.js` (D-02) for README (DOC-01, Phase 6).
</output>