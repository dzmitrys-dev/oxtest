---
phase: 01-foundations-domain-types-strict-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .nvmrc
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/tsconfig.build.json
  - apps/api/nest-cli.json
  - apps/api/eslint.config.mjs
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/app.controller.ts
  - apps/api/src/app.service.ts
autonomous: false
requirements:
  - TYPE-01
user_setup: []

must_haves:
  truths:
    - "`tsc --noEmit` (via `npm run typecheck --workspace apps/api`) exits 0 under strict:true + noUncheckedIndexedAccess against the scaffolded boilerplate"
    - "`npm ls typescript --workspace apps/api` resolves to 6.0.3 (NOT 7.0.2 latest, NOT 5.9.x)"
    - "`npm ls @nestjs/platform-express` reports no such dependency; `@nestjs/platform-fastify@11.1.28` is installed"
    - "A single `npm install` from repo root installs the apps/api workspace"
  artifacts:
    - "package.json (root) with workspaces:[\"apps/api\"]"
    - ".nvmrc pinning Node LTS"
    - "apps/api/package.json with pinned deps + build/start/dev/typecheck scripts"
    - "apps/api/tsconfig.json (strict block, overwritten not patched)"
  key_links:
    - "root workspaces array drives one install for apps/api (D-01)"
    - "strict tsconfig overwritten wholesale (not diff-patched) so strict:false default is gone (Pitfall 2)"
    - "typescript pinned exactly to 6.0.3 so typescript-eslint (<6.1.0) and ts-jest peers resolve (Pitfall 1)"
---

<objective>
Establish the npm-workspaces monorepo root and scaffold the `apps/api` NestJS 11 package onto the Fastify adapter with a strict, reviewer-idiomatic TypeScript configuration that compiles with zero errors.

Purpose: This is the foundation every later phase builds on. TYPE-01 (strict tsconfig, no `any`) is won here, and the correct TypeScript version pin (6.0.3, not the npm `latest` 7.0.2) is the single highest-risk decision in the phase — get it wrong and `typescript-eslint`/`ts-jest` peer resolution breaks on the first install.
Output: A repo root + `apps/api` workspace that installs with one `npm install`, has no Express traces, and passes `tsc --noEmit` under `strict: true` + `noUncheckedIndexedAccess`.
</objective>

<execution_context>
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/workflows/execute-plan.md
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-domain-types-strict-config/01-CONTEXT.md
@.planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md
@.planning/research/STACK.md
@.claude/CLAUDE.md
</context>

<version_reconciliation>
CLAUDE.md / .planning/research/STACK.md say TypeScript **5.9.x**. `.planning/phases/.../01-RESEARCH.md` (newer, verified live via `npm view` on 2026-07-09) supersedes this: pin **TypeScript 6.0.3**. npm `latest` is 7.0.2 (the Go-native compiler) and is INCOMPATIBLE with `typescript-eslint` (peer `>=4.8.4 <6.1.0`) and `ts-jest` (peer `>=4.3 <7`). RESEARCH.md wins on every verified version. Use the exact pins in RESEARCH.md "Standard Stack" / "Installation", NOT the STACK.md numbers where they differ.
</version_reconciliation>

<artifacts_produced>
Symbols / files this plan creates (source-grounding reference — these are net-new, not drift):
- Files: `package.json` (root), `.nvmrc`, `package-lock.json`, `apps/api/package.json`, `apps/api/tsconfig.json`, `apps/api/tsconfig.build.json`, `apps/api/nest-cli.json`, `apps/api/eslint.config.mjs`
- Scaffolded-then-temporary (replaced/removed in Plan 02): `apps/api/src/main.ts`, `apps/api/src/app.module.ts`, `apps/api/src/app.controller.ts`, `apps/api/src/app.service.ts`, `apps/api/src/app.controller.spec.ts`
- npm scripts: `build`, `start:api`, `start:worker`, `dev:api`, `dev:worker`, `typecheck`, `test`, `lint` (in apps/api/package.json)
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (blocking, pre-install)</name>
  <action>STOP before any `npm install`/`nest new`. Present the six `SUS`-flagged packages to the human and obtain explicit approval to install them at the RESEARCH.md-pinned versions. This is a security gate (T-01-SC) — never auto-approve, regardless of auto_advance. Do not proceed to Task 2 until the human types "approved".</action>
  <files>none (gate only)</files>
  <what-built>Nothing yet — this gate runs BEFORE any `npm install` or `nest new` (which itself installs packages). RESEARCH.md "Package Legitimacy Audit" flags six packages as `SUS` under a "too-new" heuristic false-positive (their most-recent publish is inside the freshness window, not the package itself being new): `@nestjs/core`, `@nestjs/common`, `@nestjs/platform-fastify`, `joi`, `typescript`, and `fastify` (transitive via platform-fastify). All six have long-lived canonical org repos and tens-to-hundreds of millions of weekly downloads. No package carries a `[SLOP]` verdict; none define a `postinstall` script.</what-built>
  <how-to-verify>
    1. Open the audit table in `.planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md` ("Package Legitimacy Audit").
    2. Confirm each flagged package resolves to its real canonical source:
       - `@nestjs/core`, `@nestjs/common`, `@nestjs/platform-fastify` -> https://www.npmjs.com/package/@nestjs/core (org: nestjs, repo github.com/nestjs/nest)
       - `joi` -> https://www.npmjs.com/package/joi (repo github.com/hapijs/joi)
       - `typescript` -> https://www.npmjs.com/package/typescript (repo github.com/microsoft/TypeScript)
       - `fastify` -> https://www.npmjs.com/package/fastify (repo github.com/fastify/fastify)
    3. Confirm you are comfortable proceeding to install these exact packages at the versions pinned in RESEARCH.md "Standard Stack".
  </how-to-verify>
  <resume-signal>Type "approved" to authorize installs, or name any package to reject.</resume-signal>
  <verify><human-check>Human reviewed the six SUS-flagged packages against their canonical npm/GitHub sources and typed "approved".</human-check></verify>
  <done>Human approval recorded; installing the RESEARCH.md-pinned package set is authorized. Never auto-approved (security gate T-01-SC).</done>
</task>

<task type="auto">
  <name>Task 2: Root workspace + scaffold apps/api on Fastify with pinned deps</name>
  <files>package.json, .nvmrc, apps/api/package.json, apps/api/nest-cli.json, apps/api/eslint.config.mjs, apps/api/src/main.ts, package-lock.json</files>
  <read_first>
    - .planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md — "Standard Stack" (exact versions), "Installation" (exact commands), Pitfall 1 (TS version), Pitfall 3/4 (Fastify swap, workspaces glob)
    - .planning/phases/01-foundations-domain-types-strict-config/01-CONTEXT.md — D-01 (npm workspaces, apps/api + apps/web), D-02 (index.ts basename), D-4b (Node not Bun, pin engines + .nvmrc), D-05 (nest new then swap to Fastify)
    - .claude/CLAUDE.md — stack constraints (NestJS 11 Fastify adapter, entry named index.ts)
  </read_first>
  <action>
    Create the repo-root `package.json` as a private workspace root with `"private": true` and `"workspaces": ["apps/api"]` — list `apps/api` explicitly, NOT an `apps/*` glob (Pitfall 4: `apps/web` will not exist with a package.json until Phase 6; a glob over a package.json-less directory has undocumented npm behavior). Add root convenience scripts that delegate to the workspace: `build`, `start:api`, `start:worker`, `dev:api`, `dev:worker`, `typecheck`, `test`, `lint`, each running the corresponding `npm run <script> --workspace apps/api`. Do NOT add runtime dependencies at the root.

    Create `.nvmrc` pinning Node 22 (LTS). This encodes D-4b: Node (V8), not Bun — `--max-old-space-size` is a V8-only flag Bun ignores, which would silently disable the 150MB heap gate the assignment grades on.

    Scaffold the backend with the Nest CLI without triggering an uncontrolled install: run `npx -y @nestjs/cli@11.0.23 new api --directory apps/api --skip-install --skip-git --package-manager npm`. This generates `apps/api/` with nest boilerplate (`src/main.ts`, `src/app.module.ts`, `src/app.controller.ts`, `src/app.service.ts`, `src/app.controller.spec.ts`, `tsconfig.json`, `tsconfig.build.json`, `nest-cli.json`, `eslint.config.mjs`, `package.json`) but does NOT install.

    Edit `apps/api/package.json` to pin the exact dependency set from RESEARCH.md before any install runs:
      - dependencies: `@nestjs/core@11.1.28`, `@nestjs/common@11.1.28`, `@nestjs/platform-fastify@11.1.28`, `@nestjs/config@4.0.4`, `joi@18.2.3`, `reflect-metadata@0.2.2`, `rxjs@7.8.2`
      - REMOVE `@nestjs/platform-express` from dependencies (D-05: swap Express -> Fastify)
      - devDependencies: `typescript@6.0.3` (exact — see version_reconciliation), `@nestjs/cli@11.0.23`, `@nestjs/schematics@11.1.0`, `@nestjs/testing@11.1.28`, `eslint@10.6.0`, `typescript-eslint@8.63.0`, `prettier@3.9.4`, `@types/node@26.1.1`, `jest@30.4.2`, `@swc/jest@0.2.39`, `tsx@4.23.0`
      - Add `"engines": { "node": ">=22 <23" }` (D-4b Node LTS pin)
      - Remove any `@types/express` / express-related devDeps the CLI may have added.

    Install once from the repo root: `npm install`. This resolves the pinned versions across the workspace and writes root `package-lock.json`.

    Update the generated `apps/api/src/main.ts` to use the Fastify adapter per RESEARCH.md "Pattern 3" (the before->after import diff): import `FastifyAdapter, NestFastifyApplication` from `@nestjs/platform-fastify` and construct via `NestFactory.create<NestFastifyApplication>(AppModule, new FastifyAdapter())`. (This file is temporary — Plan 02 replaces it with `src/index.ts` + `src/worker.ts` — but making the swap now keeps the scaffold coherent and Express-free.)

    Do NOT write any domain/business logic — this task is scaffold only.
  </action>
  <verify>
    <automated>cd /home/dzmitry/Coding/oxtest && npm ls typescript --workspace apps/api 2>/dev/null | grep -q 'typescript@6.0.3' && npm ls @nestjs/platform-fastify --workspace apps/api 2>/dev/null | grep -q '@nestjs/platform-fastify@11.1.28' && ! npm ls @nestjs/platform-express --workspace apps/api 2>/dev/null | grep -q '@nestjs/platform-express' && test -f apps/api/package.json && test -f .nvmrc && node -e "const p=require('./package.json'); if(JSON.stringify(p.workspaces)!==JSON.stringify(['apps/api'])) process.exit(1)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls typescript --workspace apps/api` shows `typescript@6.0.3` (not 7.x, not 5.9.x).
    - `npm ls @nestjs/platform-fastify --workspace apps/api` shows `@nestjs/platform-fastify@11.1.28`.
    - `npm ls @nestjs/platform-express --workspace apps/api` reports it is NOT installed.
    - Root `package.json` has `"private": true` and `"workspaces": ["apps/api"]` (explicit, no glob).
    - `.nvmrc` exists and pins Node 22; `apps/api/package.json` `engines.node` constrains to Node 22.
    - `apps/api/src/main.ts` imports `FastifyAdapter` from `@nestjs/platform-fastify` and constructs the app with it.
    - A single `npm install` at repo root completes without peer-dependency resolution errors.
  </acceptance_criteria>
  <done>Repo root is an npm workspace; apps/api is a NestJS 11 package on the Fastify adapter with every dependency pinned to the RESEARCH.md versions (TypeScript 6.0.3), no Express dependency, installed via one root `npm install`.</done>
</task>

<task type="auto">
  <name>Task 3: Overwrite tsconfig with strict block, add scripts, prove tsc clean</name>
  <files>apps/api/tsconfig.json, apps/api/tsconfig.build.json, apps/api/package.json</files>
  <read_first>
    - apps/api/tsconfig.json — the CLI-generated file (ships strict:false — must be overwritten, not patched)
    - .planning/research/STACK.md — "Recommended tsconfig (strict)" block (lines ~117-135) as the strict-flags reference
    - .planning/phases/01-foundations-domain-types-strict-config/01-RESEARCH.md — Pitfall 2 (nest new tsconfig defaults to non-strict; overwrite wholesale), Pitfall 3 (two-entrypoint scripts: build/start/dev), Open Question 1 (tsc --noEmit is the authoritative type gate, not the test transform)
    - .planning/phases/01-foundations-domain-types-strict-config/01-CONTEXT.md — Claude's Discretion (tsconfig flags beyond the mandated two; test runner Jest + @swc/jest; directory naming)
  </read_first>
  <action>
    Overwrite `apps/api/tsconfig.json` WHOLESALE (do not diff/patch — Pitfall 2). The two MANDATED flags for TYPE-01 are `"strict": true` and `"noUncheckedIndexedAccess": true`. Preserve the NestJS decorator/emit flags the framework requires: `"experimentalDecorators": true`, `"emitDecoratorMetadata": true`, `"module": "commonjs"`, `"moduleResolution": "node"`, `"target": "ES2023"`, `"outDir": "./dist"`, `"rootDir": "./src"`, `"baseUrl": "./"`, `"declaration": false`, `"sourceMap": true`, `"skipLibCheck": true`, `"esModuleInterop": true`, `"forceConsistentCasingInFileNames": true`. Add these recommended strict-adjacent flags (Claude's Discretion): `"noImplicitOverride": true`, `"noFallthroughCasesInSwitch": true`, `"noImplicitReturns": true`.

    Planner decision (document in the SUMMARY): DO NOT set `verbatimModuleSyntax` this phase. RESEARCH.md/STACK.md list it as discretionary, but it forces `import type` discipline that fights NestJS's `emitDecoratorMetadata` + CommonJS emit model and risks breaking the DI metadata NestJS reads at runtime. TYPE-01's contract is satisfied by `strict` + `noUncheckedIndexedAccess`; keeping the NestJS-idiomatic CommonJS module system keeps the scaffold reviewer-legible and the build a plain `tsc`/`nest build` with no bundler.

    Leave `apps/api/tsconfig.build.json` as the CLI default (extends tsconfig.json, excludes test/spec files) — confirm it still excludes `**/*.spec.ts` so specs do not land in `dist`.

    Add/normalize the two-entrypoint npm scripts in `apps/api/package.json` (Pitfall 3 — do NOT rely on `nest start`'s single `entryFile`):
      - `"build": "nest build"` (defaults to tsc; emits every top-level src file, so `dist/index.js` AND `dist/worker.js` both get produced once Plan 02 adds them)
      - `"start:api": "node dist/index.js"`
      - `"start:worker": "node dist/worker.js"`
      - `"dev:api": "tsx watch src/index.ts"`
      - `"dev:worker": "tsx watch src/worker.ts"`
      - `"typecheck": "tsc --noEmit"`
      - `"test": "jest"` — and set the Jest transform explicitly to `@swc/jest`: `nest new` wires a `ts-jest` transform in the `"jest"` block of `apps/api/package.json`; replace it with `"transform": { "^.+\\.(t|j)sx?$": ["@swc/jest"] }` and REMOVE `ts-jest` from devDependencies (superseded by `@swc/jest` — CONTEXT discretion, speed). Because `@swc/jest` skips type-checking during the test transform, note in SUMMARY that `tsc --noEmit` (`npm run typecheck`) is the authoritative type-safety gate for TYPE-01, run independently of `npm test` (RESEARCH.md Open Question 1 resolution)
      - `"lint": "eslint ."`

    Run `npm run typecheck --workspace apps/api` and confirm it exits 0 against the scaffolded boilerplate (Pitfall 2: verify strictness is real immediately after scaffolding, before any domain code exists).
  </action>
  <verify>
    <automated>cd /home/dzmitry/Coding/oxtest && node -e "const t=require('./apps/api/tsconfig.json').compilerOptions; if(t.strict!==true||t.noUncheckedIndexedAccess!==true) process.exit(1)" && npm run typecheck --workspace apps/api && ! grep -rnE '(:[[:space:]]*any\b|as[[:space:]]+any\b|<any>)' apps/api/src --include='*.ts' && node -e "const p=require('./apps/api/package.json'); const t=JSON.stringify((p.jest&&p.jest.transform)||{}); if(!t.includes('@swc/jest')){console.error('jest transform not using @swc/jest:',t);process.exit(1)}" && ! (node -e "const p=require('./apps/api/package.json'); process.exit((p.devDependencies&&p.devDependencies['ts-jest'])?0:1)") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/tsconfig.json` `compilerOptions.strict === true` and `compilerOptions.noUncheckedIndexedAccess === true`.
    - `grep strictNullChecks apps/api/tsconfig.json` does NOT show `false` (strict block is real, not the CLI default — Pitfall 2 warning sign).
    - `npm run typecheck --workspace apps/api` (i.e. `tsc --noEmit`) exits 0.
    - A regex scan of `apps/api/src/**/*.ts` for explicit `any` annotations / `as any` assertions returns no matches (vacuously true in the skeleton; establishes the TYPE-01 gate for later phases).
    - `apps/api/package.json` scripts include `build`, `start:api`, `start:worker`, `dev:api`, `dev:worker`, `typecheck`.
    - The Jest `transform` in `apps/api/package.json` maps to `@swc/jest` (not `ts-jest`), and `ts-jest` is absent from devDependencies.
  </acceptance_criteria>
  <done>The strict tsconfig is in force (overwritten, not patched), `tsc --noEmit` passes with zero errors, no `any` appears in src, and the two-entrypoint build/run/dev scripts exist ready for Plan 02's `index.ts`/`worker.ts`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local build/dev | Third-party packages resolved and installed into the toolchain |
| build toolchain (tsc) -> dist artifacts | Compiler + config decide what ships as runnable JS |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (`@nestjs/*`, `joi`, `typescript`, `fastify`) | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) BEFORE any `nest new`/`npm install`; install only the exact pinned versions from RESEARCH.md; none define a `postinstall` script (verified in audit) |
| T-01-01 | Tampering | TypeScript compiler version | high | mitigate | Pin `typescript@6.0.3` exactly; never `typescript@latest`/`^7` (7.0.2 mis-typechecks under embedded-API tools) — verified by `npm ls typescript` == 6.0.3 in Task 2 verify |
| T-01-02 | Information Disclosure | build/install logs | low | accept | Install logs may print package names/versions only; no secrets exist in this phase; acceptable |
</threat_model>

<verification>
- `npm install` at repo root succeeds with no peer-dependency errors.
- `npm ls typescript --workspace apps/api` == `typescript@6.0.3`.
- `@nestjs/platform-express` absent; `@nestjs/platform-fastify@11.1.28` present.
- `npm run typecheck --workspace apps/api` exits 0 under `strict: true` + `noUncheckedIndexedAccess`.
- Root `package.json` lists `workspaces: ["apps/api"]` explicitly (no glob).
</verification>

<success_criteria>
Contributes to Phase 1 Success Criterion #1 (TYPE-01): a strict, `any`-free, zero-error TypeScript baseline exists on a NestJS 11 + Fastify workspace installed via one root `npm install`, with TypeScript pinned to the tooling-compatible 6.0.3.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-domain-types-strict-config/01-01-SUMMARY.md` when done. Record: the exact installed versions (confirm TS 6.0.3), the tsconfig strict decisions (including the deliberate omission of `verbatimModuleSyntax` and why), and note that `src/main.ts` is temporary pending Plan 02's two-entrypoint restructure.
</output>
