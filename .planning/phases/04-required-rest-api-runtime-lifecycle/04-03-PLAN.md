---
phase: 04-required-rest-api-runtime-lifecycle
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - apps/api/scripts/api-integration.mjs
  - apps/api/package.json
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-04, SCAN-05, API-03, ERR-05]

must_haves:
  truths:
    - "POST /api/scan with a valid https://github.com/{owner}/{repo} URL against the COMPILED dist/index.js returns HTTP 202 with body exactly {scanId, status:'Queued'} and a Queued scan:<id> hash exists in the disposable Redis (SCAN-01, D-04)"
    - "POST /api/scan with malformed/non-GitHub URLs (each D-02 reject row) returns HTTP 400 and NO BullMQ job is enqueued and NO scan:<id> record is created — the 400-before-enqueue contract proven at the real process boundary (SCAN-02, D-01/D-02/D-03)"
    - "The full POST → Queued → Scanning → Failed(clone) chain runs end-to-end through compiled dist/index.js + dist/worker.js + Redis via the SCAN_ENGINE_TEST_FAULT=clone seam, and GET /api/scan/:scanId returns 200 with error:{category:'clone', detail} — the canonical success-criterion-#5 proof, deterministic and offline (criterion #5, SCAN-03, SCAN-04, D-05/D-07)"
    - "A Docker-backed real scan of the committed sample-repo.bundle reaches Finished and GET /api/scan/:scanId returns 200 with criticalVulnerabilities holding the two pinned CVEs (CVE-2019-10744, CVE-2021-44906) — the Finished results contract end-to-end (criterion #5 Finished branch, SCAN-03, SCAN-04, D-05/D-06); Docker-gated"
    - "GET /api/scan/<random-unknown-uuid> against the compiled API returns HTTP 404 (SCAN-05, D-05)"
    - "GET /health returns 200 {status:'ok',redis:'up',uptime} against live Redis and, after the disposable Redis is killed, returns 503 {status:'error',redis:'down',uptime} within a bounded deadline (API-03, D-08/D-09/D-10)"
    - "On SIGTERM the compiled dist/worker.js exits code 0 within SHUTDOWN_GRACE_MS + epsilon with no SIGKILL — empirically validating RESEARCH Assumption A1 (which hook @nestjs/bullmq uses to close the worker + coexistence with the custom bounded drain) and A2 (WorkerHost.worker getter reachable at destroy time); on SIGTERM the compiled dist/index.js exits code 0 cleanly (ERR-05, D-11/D-12/D-13/D-14)"
    - "The entire proof runs under node:test against the COMPILED app (dist/index.js + dist/worker.js) over a disposable redis:7-alpine container — it NEVER imports the @nestjs/bullmq-wired module into Jest (STATE.md @swc/core miette landmine)"
  artifacts:
    - "apps/api/scripts/api-integration.mjs — self-contained compiled-process + disposable-Redis integration harness (node:test) with spawnApi(), spawnWorker(), withHarness(ctx), and the REST + lifecycle + shutdown suite"
    - "apps/api/package.json — test:api:integration script (node --import tsx --test scripts/api-integration.mjs)"
  key_links:
    - "api-integration.mjs → dist/index.js (spawnApi) + dist/worker.js (spawnWorker) over disposable redis:7-alpine bound to 127.0.0.1 on an ephemeral port"
    - "POST via fetch → compiled API → ScanService.enqueue → Redis Queued hash (proves 202 + real enqueue)"
    - "worker SCAN_ENGINE_TEST_FAULT=clone + NODE_ENV=test → deterministic Failed(clone) with no network/Trivy"
    - "SIGTERM → Nest enableShutdownHooks → WorkerShutdown.onModuleDestroy(raceDrain + redis.quit) → worker process exit 0 within grace (validates A1/A2)"
  prohibitions:
    - "MUST NOT import @nestjs/bullmq or the wired ScanWorker/WorkerModule/AppModule graph into any Jest spec — this harness runs under node:test against compiled dist only"
    - "MUST NOT perform a real network clone of an external repo in CI — POST tests submit the GitHub URL as a validation input only (the 202 test spawns no worker; the Failed test uses fault=clone which rethrows before any clone), and the Docker Finished path clones ONLY the committed local sample-repo.bundle over the file transport"
    - "MUST NOT open a Redis port on any non-loopback interface — bind 127.0.0.1 only, ephemeral port, docker rm -f in a status-preserving finally"
    - "MUST NOT modify scan-engine-integration.mjs, the 04-01/04-02 plan or source files, or the Plan 02/03-owned adapter-factory.ts / env.validation.ts to force a case"
---

<objective>
Prove the end-to-end REST + runtime-lifecycle contract for Phase 4 against the COMPILED application (`dist/index.js` API + `dist/worker.js` worker) over a disposable Redis container — the integration proof that ROADMAP success criterion #5 demands, WITHOUT GraphQL and WITHOUT ever importing the `@nestjs/bullmq`-wired module into Jest (the confirmed `@swc/core` miette panic; STATE.md). This plan resolves the CONTEXT `<deferred>` "Integration-test strategy details" per RESEARCH.md "Integration / Test Strategy → Layer B".

This is the compiled-process owner for the proofs Plans 01 and 02 delegate to Plan 03:
- POST → HTTP 202 `{scanId, status:'Queued'}` (D-04); malformed/non-GitHub URL → 400 BEFORE enqueue (D-01/D-02/D-03).
- GET `/api/scan/:id` state-shaped 200 (Finished→`criticalVulnerabilities`, Failed→`error{category,detail}`) (D-05/D-06/D-07); unknown id → 404 (D-05).
- GET `/health` → 200 `{status,redis,uptime}` and 503 when Redis is down (D-08/D-09/D-10).
- SIGTERM/SIGINT → the worker drains bounded by `SHUTDOWN_GRACE_MS` then force-closes, both processes `redis.quit()` and exit cleanly within the grace window — empirically validating RESEARCH Assumptions A1 and A2 (D-11/D-12/D-13/D-14).

Purpose: Deliver ROADMAP Phase 4 success criterion #5 and give integration-level reinforcement to the contracts Plans 01/02 prove at the unit boundary.

Output: A new self-contained `apps/api/scripts/api-integration.mjs` harness (extending the proven Phase 3 `scan-engine-integration.mjs` design — same conventions, adding a `spawnApi()` dimension) plus a `test:api:integration` npm script.
</objective>

## Artifacts This Phase Produces (Plan 03 subset)

New/modified artifacts created here:
- `apps/api/scripts/api-integration.mjs` (NEW) — self-contained compiled-process + disposable-Redis integration harness (`node:test`). Lifts the proven helper shapes from `scan-engine-integration.mjs` (disposable-Redis lifecycle, `withHarness`, `spawnWorker`, status observer, terminal poll, cleanup asserts) and ADDS the API dimension.
- `spawnApi()` helper (in the new harness) — allocates a free loopback TCP port, spawns `dist/index.js` with `PORT`/`REDIS_HOST`/`REDIS_PORT`/`SCAN_TMP_DIR`, waits for the `API HTTP listener ready` stdout sentinel (`index.ts:17`), and returns `{ baseUrl, waitReady, waitExit, kill }`.
- `withHarness` ctx extended with `spawnApi` (alongside the existing `spawnWorker`/`openQueue`) and a `killRedis()` for the `/health` 503 case.
- `test:api:integration` npm script in `apps/api/package.json` → `node --import tsx --test scripts/api-integration.mjs` (mirrors `test:scan-engine:integration`).

<execution_context>
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/workflows/execute-plan.md
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-required-rest-api-runtime-lifecycle/04-CONTEXT.md
@.planning/phases/04-required-rest-api-runtime-lifecycle/04-RESEARCH.md
@.planning/phases/04-required-rest-api-runtime-lifecycle/04-PATTERNS.md

# Sibling plans this plan completes (read to align contracts — DO NOT modify)
@.planning/phases/04-required-rest-api-runtime-lifecycle/04-01-PLAN.md
@.planning/phases/04-required-rest-api-runtime-lifecycle/04-02-PLAN.md

# The Phase 3 harness to EXTEND (copy the proven shapes; do NOT modify this file)
@apps/api/scripts/scan-engine-integration.mjs

# Compiled entrypoints the harness spawns (readiness sentinels + env contract)
@apps/api/src/index.ts
@apps/api/src/worker.ts
@apps/api/src/config/env.validation.ts

# Seams the assertions read against
@apps/api/src/scan/scan.module.ts
@apps/api/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold self-contained api-integration.mjs with spawnApi() + a /health smoke test; register test:api:integration</name>
  <files>apps/api/scripts/api-integration.mjs, apps/api/package.json</files>
  <read_first>
    - `apps/api/scripts/scan-engine-integration.mjs` in full — lift verbatim the proven helper implementations: `ensureBuilt()` (67-84), `assertDockerAvailable()` (101-111), `sleep` (113), `startDisposableRedis()` (119-147), `connectRedis()` (150-170), `spawnWorker()` env block + readiness/report waiters (230-332), `seedQueued()` (172-191), `startStatusObserver()` (199-223), `waitTerminal()` (335-346), `readCriticals()` (349-352), `readFailureReason()` (355-357), `assertNoScanArtifacts()`/`walkEntries()` (360-395), and the `withHarness()` scaffold (402-445). Copy the `WORKER_READY_MARKER`/`EXPECTED_CRITICAL_IDS`/timeout constants (45-62).
    - `apps/api/src/index.ts:16-17` — the API listens on `configService.get('PORT', 3000)` bound to `0.0.0.0` and logs the exact stdout sentinel `API HTTP listener ready` on success. This is the spawnApi readiness marker (there is NO port echoed to stdout, so the harness MUST choose the port).
    - `apps/api/src/config/env.validation.ts:40-71` — the boot env contract both processes require: `REDIS_HOST`/`REDIS_PORT`/`SCAN_TMP_DIR` are `.required()` (no default), `PORT` defaults 3000, `SCAN_GIT_ALLOWED_PROTOCOLS` defaults `https`, `SCAN_ENGINE_TEST_FAULT` defaults `none`, `SHUTDOWN_GRACE_MS` (added by Plan 02) defaults 8000.
    - PATTERNS.md `apps/api/scripts/api-integration.mjs (NEW test harness)` assignment — lines 125-134 (which helpers to reuse, the `spawnApi()` addition, and the `test:api:integration` registration).
    - RESEARCH.md "Integration / Test Strategy → Layer B" — lines 423-433 (spawn BOTH compiled processes over disposable Redis; the env-var block; the offline-determinism guidance).
  </read_first>
  <action>
    Create `apps/api/scripts/api-integration.mjs` as a self-contained `node:test` harness, following the exact conventions of `scan-engine-integration.mjs` (ESM imports of `node:test`/`node:assert/strict`/`node:child_process`/`node:crypto`/`node:fs`/`node:os`/`node:path`/`node:url`, `bullmq` `Queue`, `ioredis` `Redis`; discrete argv arrays with `shell: false`; finite bounded timeouts; status-preserving `finally` teardown). Resolve `API_DIR` from `import.meta.url` and derive `API_JS = join(API_DIR, 'dist', 'index.js')` and `WORKER_JS = join(API_DIR, 'dist', 'worker.js')`. Lift the proven helpers named in `<read_first>` (do NOT import them from the Phase 3 file — copy them, matching the established codebase convention that each harness `.mjs` is self-contained; this is why scan-engine-integration.mjs stays untouched).
    Add a `spawnApi({ port, scanTmpDir, nodeEnv })` helper modeled on `spawnWorker` (scan-engine-integration.mjs:230-332) but spawning `API_JS`: env `{ ...process.env, NODE_ENV: nodeEnv ?? 'production', REDIS_HOST:'127.0.0.1', REDIS_PORT:String(port), SCAN_TMP_DIR:scanTmpDir, PORT:String(apiPort) }`, capturing stdout/stderr, resolving a `waitReady(timeoutMs)` promise when stdout includes the `API HTTP listener ready` sentinel, and exposing `baseUrl` = `http://127.0.0.1:<apiPort>`, a `waitExit(timeoutMs)` that resolves `{ code, signal }` from the child `exit` event (racing a bounded timeout that rejects), and a `kill()` that SIGKILLs if still running. Allocate `apiPort` inside the harness by opening a throwaway `node:net` server on `127.0.0.1:0`, reading `server.address().port`, then closing it before spawning (accept the small TOCTOU window as harness-acceptable) — the API needs a known port because `index.ts` does not echo the chosen port.
    Extend the `withHarness(fn)` ctx to also expose `spawnApi: (opts) => { api = spawnApi({ port: redisHandle.port, scanTmpDir, ...opts }); return api; }` and `killRedis: () => redisHandle.remove()`; in the `finally`, kill `api` (if spawned) with the same status-preserving guard used for `worker`. Keep `assertDockerAvailable()` in `withHarness` — the disposable Redis is a Docker container, so Docker remains required for every test (consistent with Phase 3 / D-29).
    Add ONE smoke `test(...)`: inside `withHarness`, `spawnApi({})`, `await api.waitReady(...)`, `fetch(`${api.baseUrl}/health`)` and assert HTTP 200 with a JSON body whose `status === 'ok'`, `redis === 'up'`, and `typeof uptime === 'number'`. This proves the spawnApi wiring, the compiled AppModule from Plans 01/03, and the live-Redis health path end-to-end.
    Modify `apps/api/package.json`: add `"test:api:integration": "node --import tsx --test scripts/api-integration.mjs"` to `scripts`, placed next to the existing `test:scan-engine:integration` entry (same invocation shape).
  </action>
  <verify>
    <automated>cd apps/api && npm run build</automated>
    <automated>cd apps/api && npm run test:api:integration</automated>
    <automated>cd apps/api && npm run test:scan-engine:integration</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:api:integration` runs the smoke test green with exit 0: the compiled API boots, emits `API HTTP listener ready`, and `GET /health` returns 200 `{status:'ok',redis:'up',uptime}`.
    - `spawnApi()` allocates a free loopback port, spawns `dist/index.js`, and exposes `baseUrl`/`waitReady`/`waitExit`/`kill`; `withHarness` ctx exposes `spawnApi` and `killRedis` and tears down the API in `finally`.
    - `npm run test:scan-engine:integration` still passes with exit 0 — proving the Phase 3 harness was NOT modified or regressed (the new harness is self-contained).
    - The disposable Redis binds `127.0.0.1` on an ephemeral port and is `docker rm -f`'d in `finally`.
  </acceptance_criteria>
  <done>A self-contained api-integration.mjs harness exists with a working spawnApi() dimension, a green /health smoke proof, and a registered npm script; the Phase 3 integration test is untouched and still green.</done>
</task>

<task type="auto">
  <name>Task 2: REST contract proofs against the compiled API — POST 202, POST 400-before-enqueue matrix, GET 404, /health 503 (D-04/D-01/D-02/D-03/D-05, API-03/D-09)</name>
  <files>apps/api/scripts/api-integration.mjs</files>
  <read_first>
    - RESEARCH.md "Integration / Test Strategy → Layer B" — lines 425-429 (POST 202 assertion, 404 assertion, /health 200/503 assertion; the `file:`-vs-`https` transport note and why POST must submit a valid GitHub URL).
    - 04-01-PLAN.md must_haves + Task 2 <behavior> — lines 24-29 and 146-159 (the exact 202 body `{scanId,status:'Queued'}`, the D-02 reject matrix rows, and the 404 mapping) so the integration assertions match the unit-level contract verbatim.
    - `apps/api/src/scan/scan.module.ts:40-47` — `REDIS_CLIENT` is a plain `new Redis({host,port})` (default ioredis retry), so after `docker rm -f` the health PING is bounded by the HealthService ~1s race (D-08) → 503.
    - The `startDisposableRedis()`/`connectRedis()`/`killRedis` helpers from Task 1 (for the 503 case that must kill Redis without breaking teardown — `docker rm -f` is idempotent, so the `finally` remove() is a harmless no-op afterward).
  </read_first>
  <action>
    Add these `node:test` cases to `api-integration.mjs`, each inside `withHarness` unless noted, each spawning ONLY the API via `ctx.spawnApi({})` (no worker — keeps every case offline and deterministic; a real GitHub URL is never actually cloned):
    POST 202 (SCAN-01, D-04): `POST ${baseUrl}/api/scan` with JSON body `{ repoUrl: 'https://github.com/OWASP/NodeGoat' }` (the assignment's NodeGoat target, used here only as a valid validation input). Assert HTTP status 202, `content-type` JSON, and body deep-equals `{ scanId: <non-empty string>, status: 'Queued' }`. Then assert the real enqueue side effect: `ctx.redis.hget(`scan:${scanId}`, 'status') === 'Queued'` and `hget(..., 'repoUrl')` equals the submitted URL. Do NOT spawn a worker, so the job stays Queued (no network clone).
    POST 400-before-enqueue (SCAN-02, D-01/D-02/D-03): table-drive over the D-02 reject rows (mirror 04-01 Task 1 <behavior>): missing body, `{}` (no repoUrl), empty string, `git@github.com:OWASP/NodeGoat.git` (SSH), `git://github.com/OWASP/NodeGoat`, `file:///etc/passwd`, `http://github.com/OWASP/NodeGoat`, `https://user:pass@github.com/OWASP/NodeGoat`, `https://github.com:8443/OWASP/NodeGoat`, `https://github.com.evil.com/OWASP/NodeGoat`, `https://github.com/OWASP` (single segment). For each: assert HTTP 400. After the whole matrix, assert the 400-before-enqueue invariant at the process boundary: `ctx.redis.keys('scan:*')` returns an empty array (no scan record was created) AND the BullMQ queue is empty (`ctx.openQueue().getJobCounts()` → all counts 0). This structurally proves `ScanService.enqueue` was never reached (D-03).
    GET 404 (SCAN-05, D-05): `GET ${baseUrl}/api/scan/${randomUUID()}` for an id never seeded → assert HTTP 404.
    /health 503 (API-03, D-09/D-10): a DEDICATED test that does NOT use the shared `withHarness` Redis lifecycle — instead call `startDisposableRedis()` + `connectRedis()` directly (or use `withHarness` and then `ctx.killRedis()`), spawn the API, assert `GET /health` → 200 first, then kill the Redis container (`ctx.killRedis()` / `redisHandle.remove()`), then poll `GET /health` until it returns HTTP 503 within a bounded deadline (e.g. 10s, poll ~250ms) and assert the body is exactly `{status:'error',redis:'down',uptime:<number>}`. Tear down the API and (idempotently) the Redis container in `finally`.
    Reuse Task 1's constants/helpers; keep every wait bounded and every disposable resource cleaned in a status-preserving `finally`.
  </action>
  <verify>
    <automated>cd apps/api && npm run test:api:integration</automated>
  </verify>
  <acceptance_criteria>
    - POST valid GitHub URL → 202 with body `{scanId,status:'Queued'}` and a `scan:<id>` Queued hash present in Redis (SCAN-01, D-04).
    - Every D-02 reject row → 400, and after the matrix there are zero `scan:*` keys and zero queued BullMQ jobs (SCAN-02, D-01/D-02/D-03 — 400 before enqueue proven at the compiled boundary).
    - GET unknown uuid → 404 (SCAN-05).
    - `/health` returns 200 with live Redis and transitions to 503 `{status:'error',redis:'down',uptime}` within the bounded deadline after Redis is killed (API-03, D-09/D-10).
    - `npm run test:api:integration` exits 0 with all cases green and no leaked containers/ports.
  </acceptance_criteria>
  <done>The compiled API's REST surface (202, 400-before-enqueue matrix, 404, health 200/503) is proven end-to-end over disposable Redis.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end criterion #5 (POST→Queued→poll→Failed via fault; Docker Finished at GET) + graceful SIGTERM shutdown for worker and API, empirically validating A1/A2 (ERR-05, D-11..D-14, SCAN-03/04, D-05/06/07)</name>
  <files>apps/api/scripts/api-integration.mjs</files>
  <read_first>
    - RESEARCH.md "Integration / Test Strategy → Layer B" — lines 427, 430-433 (the offline `POST→202→Queued→Scanning→Failed(clone)` via injected fault; the Docker-backed Finished happy path; the SIGTERM-mid-scan exit-within-grace requirement) and Assumptions A1/A2 — lines 393-394, Open Question 1 lines 401-404 (what the SIGTERM test must empirically validate).
    - 04-02-PLAN.md must_haves + Task 2 acceptance — lines 18-24 and 131-138 (the drain/force/`redis.quit`/`SHUTDOWN_GRACE_MS` contract this task proves at runtime; the explicit delegation of the end-to-end SIGTERM proof to Plan 03).
    - `apps/api/scripts/scan-engine-integration.mjs` success test (447-521) and fault test (558-619) — the exact `seedQueued` + `openQueue().add('scan', {scanId, repoUrl})` + `startStatusObserver` + `waitTerminal` + `readCriticals`/`readFailureReason` + `assertNoScanArtifacts` pattern to reuse; `EXPECTED_CRITICAL_IDS` (59) and the `nodeEnv:'test'` fault-activation rule (HIGH-02, 237-254).
    - `apps/api/src/worker.ts:17,27` — `enableShutdownHooks()` is present and the worker emits `SCAN_WORKER_READY`; the standalone context stays alive until SIGTERM, when the Plan 02 `WorkerShutdown.onModuleDestroy` runs `raceDrain(host.worker, SHUTDOWN_GRACE_MS)` then `redis.quit()`.
    - `apps/api/src/index.ts:14,17` — the API `enableShutdownHooks()` + the Plan 02 `ScanRepositoryAdapter.onModuleDestroy → redis.quit()` give it a clean exit (D-14).
  </read_first>
  <action>
    Add these `node:test` cases to `api-integration.mjs`:
    Criterion #5 — POST → Queued → poll → Failed (DETERMINISTIC, offline; SCAN-03/04, D-05/D-07): inside `withHarness`, spawn BOTH `ctx.spawnApi({})` and `ctx.spawnWorker({ fault: 'clone', readyMarker: 'none', nodeEnv: 'test' })` (fault seam is inert in production — HIGH-02 — so the worker MUST run under `nodeEnv:'test'`; the API stays default). `await` both readiness markers. `POST ${baseUrl}/api/scan` with `{ repoUrl: 'https://github.com/OWASP/NodeGoat' }` → assert 202 `{scanId,status:'Queued'}`. Start `startStatusObserver(ctx.port, scanId)`. Poll `GET ${baseUrl}/api/scan/${scanId}` (every ~100ms, bounded) until the response body `status === 'Failed'`; assert the GET returns HTTP 200 with body `{scanId, status:'Failed', error:{category:'clone', detail:<non-empty string ≤500 chars>}}` (D-05/D-07). Stop the observer and assert the exact lifecycle `['Queued','Scanning','Failed']`. The clone double rethrows before any network call, so NO real GitHub clone occurs — this is the canonical, offline success-criterion-#5 proof through the REST boundary.
    Criterion #5 — Docker Finished at the GET boundary (DOCKER-GATED; SCAN-03/04, D-05/D-06): inside `withHarness`, `seedQueued(ctx.redis, scanId, SAMPLE_BUNDLE)` and `ctx.spawnWorker({ fault:'none', readyMarker:'log', nodeEnv:'production' })` with `SCAN_GIT_ALLOWED_PROTOCOLS` widened to `https:file` (already the spawnWorker default env from the copied Phase 3 block) so the worker clones the committed local `sample-repo.bundle` and runs real Docker Trivy → Finished. `ctx.openQueue().add('scan', {scanId, repoUrl: SAMPLE_BUNDLE})`, `waitTerminal(...)` → `Finished`, then also spawn `ctx.spawnApi({})` and `GET ${baseUrl}/api/scan/${scanId}` → assert HTTP 200 with `status:'Finished'` and `criticalVulnerabilities` being an array whose `vulnerabilityId`s equal `EXPECTED_CRITICAL_IDS` in report order, each `severity:'CRITICAL'`. Give this test the long Docker/Trivy timeout (`SCAN_TERMINAL_TIMEOUT_MS` + margin, as in the Phase 3 success test). Because Docker + Trivy image pull is required (beyond the Redis container), keep the same Docker precondition the harness already enforces; if the runner cannot pull the Trivy image, document the limitation in the SUMMARY (Phase 3 D-29 precedent) rather than weakening the assertion.
    Graceful shutdown — worker SIGTERM (ERR-05, D-11/D-12/D-13; validates A1/A2): inside `withHarness`, `ctx.spawnWorker({ fault:'none', readyMarker:'none', nodeEnv:'production' })`, `await worker.waitReady(...)` (idle worker, no job enqueued — deterministic, no Docker/Trivy). Send `worker.child.kill('SIGTERM')`, then `await worker.waitExit(SHUTDOWN_GRACE_MS + margin)` and assert `{ code: 0, signal: null }` — a clean exit within the grace window with NO SIGKILL. State in a comment that this empirically validates RESEARCH A1 (the custom bounded `raceDrain` coexists with @nestjs/bullmq's own teardown) and A2 (the `WorkerHost.worker` getter is reachable at destroy — a broken getter would throw in `onModuleDestroy`, blocking the clean exit-0 within grace this asserts).
    Graceful shutdown — API SIGTERM (ERR-05, D-14): inside `withHarness`, `ctx.spawnApi({})`, `await api.waitReady(...)`, `api.child.kill('SIGTERM')`, `await api.waitExit(shortBound)` and assert `{ code: 0, signal: null }` — the API stops accepting requests and `ScanRepositoryAdapter.onModuleDestroy → redis.quit()` lets it exit cleanly (no SIGKILL needed).
  </action>
  <verify>
    <automated>cd apps/api && npm run test:api:integration</automated>
  </verify>
  <acceptance_criteria>
    - The offline POST→202→poll path progresses `Queued → Scanning → Failed` and GET returns 200 `{scanId,status:'Failed',error:{category:'clone',detail}}` — success criterion #5 proven deterministically without network or Trivy (SCAN-03/04, D-05/D-07).
    - The Docker Finished path reaches `Finished` and GET returns 200 with `criticalVulnerabilities` = the two pinned CVEs in report order (criterion #5 Finished branch, SCAN-03/04, D-05/D-06); if Docker/Trivy is unavailable the limitation is documented, not silently skipped.
    - On SIGTERM the compiled worker exits `{code:0, signal:null}` within `SHUTDOWN_GRACE_MS + margin` (no SIGKILL), empirically validating A1 and A2 (ERR-05, D-11/D-12/D-13).
    - On SIGTERM the compiled API exits `{code:0, signal:null}` within a short bound (ERR-05, D-14).
    - `npm run test:api:integration` exits 0 with the full suite green and no leaked containers, ports, or clone/report artifacts.
  </acceptance_criteria>
  <done>Success criterion #5 (POST → Queued → poll → Finished/Failed) and graceful SIGTERM shutdown of both compiled processes are proven end-to-end over disposable Redis, empirically validating RESEARCH Assumptions A1/A2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → disposable Redis container | Test infra provisions a real Redis; the port must never be exposed off-loopback and the container must always be torn down. |
| harness → compiled processes (dist/index.js, dist/worker.js) | Spawned as real OS processes with a controlled env; a stuck child must be bounded and killed. |
| test input → compiled API (POST body) | The NodeGoat GitHub URL is submitted only as a validation input string; it must NOT trigger a real network clone in CI. |
| worker → git clone transport | Only the committed local `sample-repo.bundle` may be cloned (file transport, trusted infra); production stays https-only. |

## STRIDE Threat Register (ASVS L1, block-on-high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Information disclosure / Availability | disposable Redis container exposure / leak | low | mitigate | Start Redis with `-p 127.0.0.1:0:6379` (loopback + ephemeral port, copied from scan-engine-integration.mjs:119-147) and `docker rm -f` it in a status-preserving `finally`; `killRedis()`/remove() is idempotent. Checkable: no leaked container after the suite. |
| T-04-11 | Tampering / SSRF (real external clone in CI) | POST tests submitting a GitHub URL | medium | mitigate | No test performs a real network clone: the 202/400/404 tests spawn NO worker; the Failed test uses `SCAN_ENGINE_TEST_FAULT=clone` (double rethrows before any clone); the Docker Finished path clones ONLY the committed local `sample-repo.bundle` over the `file` transport (trusted infra). Checkable: the fault/offline cases run with no network. |
| T-04-12 | Information disclosure (secrets in test infra) | harness env / fixtures | low | mitigate | The harness uses only synthetic fixtures (`sample-repo.bundle`) and loopback Redis; no real credentials, tokens, or `.env` secrets are read or committed. |
| T-04-13 | Denial of service (runaway child process) | spawned dist/index.js + dist/worker.js | low | mitigate | Every wait (readiness, terminal poll, `waitExit`) is bounded by a finite timeout; children are SIGKILLed in `finally` if still alive (mirrors scan-engine-integration.mjs kill() guard). |
| T-04-SC | Tampering (supply chain) | npm installs | low | accept | No package-manager installs in this plan — the harness uses only already-installed `node:test`, `bullmq`, `ioredis`, and `tsx`. No new supply-chain surface, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required. |
</threat_model>

<verification>
- `cd apps/api && npm run build` succeeds (compiles Plans 01/02/03 output into `dist/index.js` + `dist/worker.js`).
- `cd apps/api && npm run test:api:integration` exits 0 with the full suite green: /health smoke, POST 202, POST 400-before-enqueue matrix (zero `scan:*` keys + empty queue after), GET 404, /health 503, POST→Queued→poll→Failed(clone), Docker Finished→GET criticalVulnerabilities, worker SIGTERM exit-0-within-grace, API SIGTERM exit-0.
- `cd apps/api && npm run test:scan-engine:integration` still exits 0 — the Phase 3 harness is untouched (proves no cross-file regression).
- The harness runs under `node --import tsx --test` (NOT Jest) and never imports `@nestjs/bullmq`/`ScanWorker`/`WorkerModule`/`AppModule` — honoring the STATE.md `@swc/core` miette landmine.
- No leaked Docker containers, bound ports, or clone/report artifacts remain after the run (status-preserving `finally` teardown).
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion #5 is met: an integration test proves `POST /api/scan → Queued → poll → Finished/Failed` against the compiled worker/service boundary over disposable Redis, without GraphQL.
- The compiled-process proofs delegated by Plans 01 and 02 are green: 202 `{scanId,status:'Queued'}`; 400 before enqueue; state-shaped 200 with `criticalVulnerabilities` (Finished) / `error{category,detail}` (Failed); 404; `/health` 200 and 503; SIGTERM clean exit within `SHUTDOWN_GRACE_MS` for both processes.
- RESEARCH Assumptions A1 (@nestjs/bullmq close-hook coexistence with the bounded drain) and A2 (`WorkerHost.worker` getter reachable at destroy) are empirically validated by the worker SIGTERM exit-0-within-grace assertion.
- The proof runs against compiled `dist/` under `node:test`, never importing the `@nestjs/bullmq`-wired module into Jest.
</success_criteria>

## Source Coverage (Plan 03 scope)

This plan is the integration-boundary completion of Phase 4; Plans 01/02 own the unit boundary. This plan's charge maps cleanly to every source item it is responsible for — no gaps.

- **GOAL** (ROADMAP Phase 4 success criterion #5): COVERED — Task 3 (POST→Queued→poll→Failed via fault; Docker Finished at GET).
- **REQ** integration-level reinforcement: SCAN-01 (Task 2 POST 202), SCAN-02 (Task 2 400 matrix), SCAN-03/SCAN-04 (Task 3 Failed/Finished GET), SCAN-05 (Task 2 404), API-03 (Task 1 /health 200 smoke + Task 2 503), ERR-05 (Task 3 SIGTERM worker + API). ARCH-01 is proven at the unit boundary by Plan 01's import-guard — out of this plan's scope.
- **RESEARCH** (Layer B compiled-process + disposable-Redis harness; Assumptions A1/A2): COVERED — Task 1 scaffold, Task 3 empirical A1/A2 validation.
- **CONTEXT** (D-04 / D-01-03 / D-05-07 / D-08-10 / D-11-14 integration reinforcement): COVERED across Tasks 1-3; CONTEXT `<deferred>` "Integration-test strategy details" is resolved here.

<output>
Create `.planning/phases/04-required-rest-api-runtime-lifecycle/04-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
