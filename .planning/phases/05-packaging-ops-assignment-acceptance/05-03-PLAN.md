---
phase: 05-packaging-ops-assignment-acceptance
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - apps/api/scripts/acceptance.mjs
  - apps/api/scripts/docker-oom-proof.mjs
  - apps/api/package.json
  - .github/workflows/scan-engine.yml
  - .github/CI-CONTRACT.md
autonomous: true
requirements: [OPS-05, OPS-02]
tags: [acceptance, ci, memory-proof, oom, integration, ops]

must_haves:
  truths:
    - "The acceptance command proves POST /api/scan -> Queued -> worker scan -> poll -> CRITICAL results, then asserts clone/report cleanup after BOTH success and a forced failure (criterion #1)"
    - "A single scan's API log line and worker log line carry the same scanId, so the lifecycle is reconstructable across the two processes (OPS-04 end-to-end, criterion #3)"
    - "`node --max-old-space-size=150 dist/index.js` boots the API cleanly without OOM (criterion #5a); `dist/worker.js` boots clean under the same ceiling; the 500MB+ parse survives 150MB via the reused Phase-2 memtest (criterion #5b)"
    - "If Docker is available, the worker container survives the largest fixture under mem_limit:200m + --max-old-space-size=150 with OOMKilled==false AND exit code 0 (criterion #2)"
    - "CI runs lint + type-check + parser/adapter/worker unit + REST-contract + acceptance jobs and fails the build on any failure; the Node-22 memory proof (memory.yml) stays a required gate (criterion #4)"
  artifacts:
    - "apps/api/scripts/acceptance.mjs — node:test end-to-end + correlation + criterion-#5 harness"
    - "apps/api/scripts/docker-oom-proof.mjs — compose-driven in-container OOMKilled:false AND exit-0 proof"
    - "package.json scripts test:acceptance and test:oom:container"
    - "Extended .github/workflows/scan-engine.yml (REST-contract + acceptance + in-container-OOM jobs) and updated .github/CI-CONTRACT.md"
  key_links:
    - "acceptance.mjs -> spawns dist/index.js + dist/worker.js over disposable redis:7-alpine (reuse api-integration.mjs helpers)"
    - "docker-oom-proof.mjs -> docker compose build worker -> run memtest in-container under mem_limit:200m -> docker inspect OOMKilled+ExitCode"
    - "scan-engine.yml feasibility-gated jobs -> test:api:integration, test:acceptance, test:oom:container (required-when-run); memory.yml untouched (always-required)"
---

<objective>
Deliver the assignment-level acceptance gate (criteria #1, #3, #5), the in-container OOM proof (criterion #2), and the CI wiring that gates the whole suite (criterion #4, OPS-05). The acceptance harness clones the proven `api-integration.mjs` helpers to drive the compiled `dist/index.js` + `dist/worker.js` over a disposable Redis, proving the full REST flow to CRITICAL results plus cleanup on success AND forced failure, asserting scanId correlation across both processes, and reconciling the PDF's verbatim `node --max-old-space-size=150 dist/index.js` self-test with the two-entrypoint reality. A separate compose-driven step proves the worker container survives the largest fixture with `OOMKilled:false` AND exit 0.

Purpose: Turn "submission-ready" into a single runnable, CI-gated command. This is the phase's proof layer — it depends on the logging seam (Plan 01) and the Docker stack (Plan 02).

Output: `scripts/acceptance.mjs`, `scripts/docker-oom-proof.mjs`, two new npm scripts, an extended `scan-engine.yml`, and an updated `CI-CONTRACT.md`. `memory.yml` stays untouched.
</objective>

<execution_context>
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/workflows/execute-plan.md
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-ops-assignment-acceptance/05-CONTEXT.md
@.planning/phases/05-packaging-ops-assignment-acceptance/05-RESEARCH.md
@.planning/phases/05-packaging-ops-assignment-acceptance/05-PATTERNS.md
@.planning/phases/05-packaging-ops-assignment-acceptance/05-01-PLAN.md
@.planning/phases/05-packaging-ops-assignment-acceptance/05-02-PLAN.md
</context>

<artifacts>
## Artifacts this plan produces
- **New file** `apps/api/scripts/acceptance.mjs` — `node:test` harness (clones `api-integration.mjs` helpers: `ensureBuilt`, `ensureFixture`, `startDisposableRedis`, `connectRedis`, `spawnApi`, `spawnWorker`, `startStatusObserver`, `waitTerminal`, `readCriticals`, `assertNoScanArtifacts`, `withHarness`, `isDockerAvailable`). Cases: (a) happy path POST->Queued + worker scan->Finished->CRITICAL->cleanup; (b) forced-failure POST->Failed(clone)->cleanup; (c) scanId correlation across API+worker stdout; (d) criterion #5 boots + reused memtest.
- **New file** `apps/api/scripts/docker-oom-proof.mjs` — builds the worker image via compose, runs the compiled `dist/scripts/memtest.js` inside the worker container under `mem_limit:200m` + `--max-old-space-size=150` against a >=500MB fixture, then asserts `docker inspect --format '{{.State.OOMKilled}} {{.State.ExitCode}}'` equals `false 0` and surfaces peak RSS/heapUsed. Feasibility-gated.
- **New scripts** in `apps/api/package.json` — `test:acceptance` (`node --import tsx --test scripts/acceptance.mjs`) and `test:oom:container` (`node --import tsx scripts/docker-oom-proof.mjs`).
- **Extended** `.github/workflows/scan-engine.yml` — wire `test:api:integration` (currently ungated), add `test:acceptance` and the in-container OOM job as feasibility-gated (required-when-run); Docker-free additions stay in the always-required contract job.
- **Updated** `.github/CI-CONTRACT.md` — new status rows classified feasibility-gated (required-when-run), preserving fail-closed-on-unknown semantics.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: acceptance.mjs — end-to-end REST flow, scanId correlation, and criterion-#5 proof</name>
  <files>apps/api/scripts/acceptance.mjs, apps/api/package.json</files>
  <read_first>
    - apps/api/scripts/api-integration.mjs (the near-clone template — reuse `ensureBuilt` 73-91, `ensureFixture` 94-106, `startDisposableRedis` 136-165, `connectRedis` 168-192, `seedQueued` 195-213, `startStatusObserver` 221-245, `spawnWorker` 272-388, `spawnApi` 395-468, `waitTerminal` 471-482, `readCriticals` 485-488, `assertNoScanArtifacts` 514-524, `withHarness` 571-627, `isDockerAvailable` 121-128; markers `API HTTP listener ready`, `SCAN_WORKER_READY`; `EXPECTED_CRITICAL_IDS` line 68; the Docker Finished test 857-899; the forced-clone-failure test 792-855)
    - apps/api/src/index.ts (prints `API HTTP listener ready`; boots under whatever flags the argv passes — the criterion-#5a boot proof spawns it with `--max-old-space-size=150`)
    - apps/api/src/worker.ts (prints `SCAN_WORKER_READY`)
    - apps/api/scripts/memtest.ts + apps/api/scripts/memory-threshold.ts (compiled to `dist/scripts/memtest.js`; RSS threshold 240MB; env `MEMTEST_EXPECTED_CRITICAL_COUNT`)
    - .github/workflows/memory.yml (how a >=512MiB fixture is generated + memtest invoked: `npm run gen:fixture -- --size-mb 512 --output <path>` then `node --max-old-space-size=150 dist/scripts/memtest.js <path>`)
    - apps/api/package.json (scripts block 11-30 — add `test:acceptance` mirroring `test:api:integration`)
    - .planning/phases/05-packaging-ops-assignment-acceptance/05-PATTERNS.md "acceptance.mjs (new — D-08, D-09)" (happy/forced-failure/criterion-#5 recipe) and .planning/.../05-RESEARCH.md D-09/D-10 + Open Question 2
    - .planning/phases/05-packaging-ops-assignment-acceptance/05-01-PLAN.md (the pino ndjson `scanId` field emitted by API enqueue + worker lifecycle lines that the correlation case parses)
  </read_first>
  <action>
    Author `scripts/acceptance.mjs` reusing the `api-integration.mjs` helper shapes VERBATIM (do NOT re-invent spawn/teardown — "Don't Hand-Roll"). Add these `node:test` cases:
    (1) HAPPY PATH — `POST /api/scan` with a valid GitHub URL returns 202 `{scanId, status:Queued}` (real HTTP boundary, deterministic); then prove a worker scan reaches Finished with the CRITICAL results returned by `GET /api/scan/:id` and assert `assertNoScanArtifacts` (cleanup on success). Use the deterministic Docker path from api-integration.mjs:857-899 (seed the committed `sample-repo.bundle` + real pinned Trivy -> the two `EXPECTED_CRITICAL_IDS`) for the scan->CRITICAL half; the POST->Queued half proves the REST enqueue. This whole case is Docker-gated via `isDockerAvailable()` + `t.skip(reason)` (D-12).
    (2) FORCED-FAILURE PATH — POST a valid URL -> spawn worker `{ fault:'clone', nodeEnv:'test' }` (activates the SCAN_ENGINE_TEST_FAULT seam, HIGH-02) -> poll `GET` until `Failed` with `error.category==='clone'` -> `assertNoScanArtifacts` (cleanup on failure). Deterministic/offline.
    (3) scanId CORRELATION (OPS-04, criterion #3) — capture `api.state.stdout` and `worker.state.stdout` from a single scan; parse ndjson lines; assert at least one API line AND at least one worker line have a JSON `scanId` field equal to the scan's id (i.e. `... | jq 'select(.scanId==id)'` finds a line in each stream). If the worker's non-dev pino output is on stdout, parse from there.
    (4) CRITERION #5 — (a) spawn `dist/index.js` via argv `[ '--max-old-space-size=150', API_JS ]` (mirror `spawnApi` at line 397 but prepend the flag) and assert it emits `API HTTP listener ready` with no non-zero/OOM exit; (a') spawn `dist/worker.js` under the same flag and assert `SCAN_WORKER_READY`; (b) generate a >=500MB fixture via `npm run gen:fixture -- --size-mb 512 --output <tmp>` and run `node --max-old-space-size=150 dist/scripts/memtest.js <tmp>` (compiled Phase-2 memtest, reused verbatim per D-10) asserting exit 0, then delete the fixture. Guard case (b) behind an env knob (e.g. `ACCEPTANCE_FULL_MEMPROOF`) defaulting ON in CI but allowing a smaller size locally — the >=500MB default is what CI runs.
    Add `"test:acceptance": "node --import tsx --test scripts/acceptance.mjs"` to `apps/api/package.json`. Every subprocess uses a discrete argv array with `shell:false` and a finite bounded timeout (reuse the timeout constants). Keep this `.mjs` self-contained (the codebase convention — do not import from `api-integration.mjs`).
  </action>
  <verify>
    <automated>cd apps/api && npm run build && node --import tsx --test scripts/acceptance.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:acceptance --workspace apps/api` exits 0 (all node:test cases pass; Docker-gated cases skip-with-reason when Docker is absent per D-12, never fail closed).
    - Happy path: `POST /api/scan` returns 202 `{scanId, status:Queued}`; a worker scan reaches `Finished` and `GET /api/scan/:id` returns the two pinned CRITICAL CVEs; `assertNoScanArtifacts` passes (no `repo/` or `report.json` under SCAN_TMP_DIR after success).
    - Forced-failure: the scan reaches `Failed` with `error.category==='clone'` and `assertNoScanArtifacts` passes (cleanup on failure).
    - Correlation: the test parses at least one API ndjson line AND at least one worker ndjson line whose `scanId` field equals the scan id (proves OPS-04 cross-process trace).
    - Criterion #5: `dist/index.js` emits `API HTTP listener ready` under `--max-old-space-size=150` with no non-zero exit; `dist/worker.js` emits `SCAN_WORKER_READY` under the same flag; `dist/scripts/memtest.js` exits 0 under `--max-old-space-size=150` against a >=500MB fixture.
  </acceptance_criteria>
  <done>A single `test:acceptance` command proves the full REST-to-CRITICAL flow, cleanup on success and forced failure, scanId correlation across both processes, and the two-part criterion-#5 memory reconciliation.</done>
</task>

<task type="auto">
  <name>Task 2: docker-oom-proof.mjs — in-container OOMKilled:false AND exit-0 proof</name>
  <files>apps/api/scripts/docker-oom-proof.mjs, apps/api/package.json</files>
  <read_first>
    - docker-compose.yml (Plan 02 output — the `worker` service: build target, `mem_limit: 200m`, `--max-old-space-size=150 dist/worker.js`, working_dir)
    - Dockerfile (Plan 02 output — runtime target containing `dist/scripts/memtest.js`)
    - .planning/phases/05-packaging-ops-assignment-acceptance/05-RESEARCH.md "Pattern 5: In-container OOM proof" (assert BOTH OOMKilled==false AND exit 0 — the exit-137 false-negative guard, Pitfall 2) and "Open Question 2" (dedicated compose-driven step, feasibility-gated)
    - apps/api/scripts/api-integration.mjs (`isDockerAvailable` 121-128, `spawnSync` argv-array/`shell:false`/bounded-timeout conventions to mirror)
    - apps/api/scripts/scan-engine-feasibility.mjs (the feasibility semantics: cleanly-infeasible -> exit 0 + reason; unexpected error -> exit 1 fail-closed)
    - .github/workflows/memory.yml (the >=512MiB fixture generation recipe to reuse)
  </read_first>
  <action>
    Author `scripts/docker-oom-proof.mjs` (a `node --import tsx` script, not necessarily node:test). Steps, all via `spawnSync` argv arrays with `shell:false` + bounded timeouts: (1) feasibility preflight — if `docker`/`docker compose` is unavailable, print a machine-readable `{ feasible:false, reason }`, exit 0 (skip, do NOT fail closed); on an UNEXPECTED probe error exit 1 (fail closed) — mirror `scan-engine-feasibility.mjs`. (2) `docker compose build worker` (reuse the Plan-02 image). (3) Generate a >=500MB fixture on the host (`npm run gen:fixture -- --size-mb 512 --output <tmp>`). (4) Run a ONE-SHOT worker container under the graded constraint — `docker run --name <unique> --memory=200m --entrypoint node <worker-image> --max-old-space-size=150 dist/scripts/memtest.js /fixtures/report.json` with the host fixture bind-mounted read-only to `/fixtures/report.json` and `working_dir` set so `dist/scripts/memtest.js` resolves (mirror the compose worker). (5) `docker inspect --format '{{.State.OOMKilled}} {{.State.ExitCode}}' <name>` and REQUIRE the output equals `false 0` — fail if either OOMKilled is true OR exit code is non-zero (Pitfall 2 false-negative guard). (6) Surface the memtest stdout metrics (peak RSS/heapUsed) and always `docker rm -f <name>` + delete the fixture in a `finally`. Add `"test:oom:container": "node --import tsx scripts/docker-oom-proof.mjs"` to `apps/api/package.json`. This proves criterion #2's "largest fixture survives under mem_limit:200m with --max-old-space-size=150, OOMKilled:false" against the memory-critical parse path without needing a full network scan.
  </action>
  <verify>
    <automated>cd apps/api && node --import tsx scripts/docker-oom-proof.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - When Docker is available: the script exits 0 and its output contains `false 0` from `docker inspect` (worker survived the >=500MB fixture under `--memory=200m` + `--max-old-space-size=150`, no OOM-kill) plus peak RSS/heapUsed metrics.
    - When Docker is unavailable: the script exits 0 with `{ feasible:false, reason }` (skip-with-reason, D-12); an unexpected probe error exits 1 (fail closed).
    - The proof asserts BOTH conditions — never OOMKilled alone (Pitfall 2): `grep -q "ExitCode" apps/api/scripts/docker-oom-proof.mjs` and the inspect format string includes `.State.OOMKilled` and `.State.ExitCode`.
    - The container and fixture are removed regardless of outcome (a `finally`/teardown block exists).
  </acceptance_criteria>
  <done>A compose-driven step proves the worker container survives the largest fixture under the graded memory constraint with OOMKilled:false AND exit 0, feasibility-gated and self-cleaning.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the new jobs into scan-engine.yml and update CI-CONTRACT.md</name>
  <files>.github/workflows/scan-engine.yml, .github/CI-CONTRACT.md</files>
  <read_first>
    - .github/workflows/scan-engine.yml (the `scan-engine-contract` always-required job 13-53 and the `scan-engine-integration` feasibility-gated job 55-81 — the exact template: `if: needs.scan-engine-contract.outputs.feasible == 'true'`, checkout -> setup-node@22 -> assert Node 22 -> npm ci -> build -> run script)
    - .github/CI-CONTRACT.md (status table 10-13, per-status prose 15-40, feasibility semantics 42-53, policy notes 55-65 — the format to extend, keeping "never treat unknown as success")
    - .github/workflows/memory.yml (KEEP UNTOUCHED — the always-required Node-22 memory proof; reference it in CI-CONTRACT as the criterion-#5b 500MB gate)
    - apps/api/package.json (the scripts to wire: existing `test:api:integration` currently ungated; new `test:acceptance` and `test:oom:container` from Tasks 1-2)
    - .planning/phases/05-packaging-ops-assignment-acceptance/05-RESEARCH.md D-11/D-12 (tiered required-status) and 05-PATTERNS.md "scan-engine.yml (extend...)" + "CI-CONTRACT.md"
  </read_first>
  <action>
    Extend `.github/workflows/scan-engine.yml` WITHOUT touching `memory.yml`. Keep `scan-engine-contract` (always-required) and `scan-engine-integration` as-is. Add feasibility-gated jobs that reuse the `scan-engine-integration` block shape (`needs: scan-engine-contract`, `if: needs.scan-engine-contract.outputs.feasible == 'true'`, checkout -> setup-node@22 -> assert Node 22 -> npm ci -> build -> run): one wiring the currently-ungated `test:api:integration` (REST contract), one wiring `test:acceptance`, and one wiring `test:oom:container` (in-container OOM). Docker-free static additions (if any) belong in the always-required contract job's step list. Update `.github/CI-CONTRACT.md`: add status rows for `scan-engine-api-integration`, `scan-engine-acceptance`, and `scan-engine-oom` (or the chosen job names) classified feasibility-gated (required-when-run, skipped-with-reason otherwise); add a note that `memory.yml`'s `node-22-memory` remains the separate always-required Node-22 500MB memory proof (criterion #5b). Keep the "Only always-run jobs are safe to mark required; unknown probe state fails the contract job closed" policy language verbatim in spirit. Job names in the workflow MUST match the identifiers documented in CI-CONTRACT.md (the reviewable source of truth).
  </action>
  <verify>
    <automated>node -e "const y=require('/home/dzmitry/Coding/oxtest/apps/api/node_modules/js-yaml');const wf=y.load(require('fs').readFileSync('.github/workflows/scan-engine.yml','utf8'));const jobs=Object.keys(wf.jobs);if(jobs.length<4)throw new Error('expected >=4 jobs, got '+jobs.join(','));const txt=require('fs').readFileSync('.github/workflows/scan-engine.yml','utf8');for(const s of ['test:api:integration','test:acceptance','test:oom:container'])if(!txt.includes(s))throw new Error('missing wiring: '+s);console.log('CI_WIRED '+jobs.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - `scan-engine.yml` parses as valid YAML with >=4 jobs and wires `test:api:integration`, `test:acceptance`, and `test:oom:container` (grep each string present).
    - Every new Docker/Redis-backed job carries `if: needs.scan-engine-contract.outputs.feasible == 'true'` (required-when-run, skipped otherwise) — `grep -c "feasible == 'true'" .github/workflows/scan-engine.yml` is >= 3 (integration + the new gated jobs).
    - `.github/workflows/memory.yml` is byte-identical to its pre-task state: `git diff --exit-code .github/workflows/memory.yml` shows no change.
    - `CI-CONTRACT.md` lists the new job identifiers and they match the workflow job names exactly; it still states unknown probe state fails the contract job closed.
  </acceptance_criteria>
  <done>CI gates lint + type-check + unit + REST-contract + acceptance + in-container-OOM (feasibility-gated where Docker-backed), the Node-22 memory proof stays a separate required gate, and CI-CONTRACT.md is the accurate reviewable source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ↔ Docker/Redis/Trivy | Feasibility-gated jobs orchestrate real containers; an unknown state must not read as success |
| test harness ↔ compiled processes | The harness spawns real `dist/index.js`/`dist/worker.js` and the SCAN_ENGINE_TEST_FAULT seam |
| container ↔ memory cgroup | The OOM proof measures survival under `--memory=200m` |
| CI logs/artifacts ↔ reviewers | Feasibility JSON + step logs are surfaced publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Tampering | CI treating unknown/infeasible as success | high | mitigate | Reuse the fail-closed feasibility probe: cleanly-infeasible -> exit 0 + recorded reason (skip); unexpected error -> exit 1 (fail the contract job closed); required-when-run per CI-CONTRACT.md (D-12) |
| T-05-03-02 | Information disclosure | secrets/tokens in CI logs or the OOM-proof output | medium | mitigate | Harness uses ephemeral loopback + disposable Redis; no secrets echoed; OOM-proof output is limited to `OOMKilled ExitCode` + RSS metrics; no report bodies logged (V7) |
| T-05-03-03 | Information disclosure | OOM false-negative (exit 137 with OOMKilled:false) | high | mitigate | Assert BOTH `OOMKilled == false` AND exit code 0 (Pattern 5 / Pitfall 2) |
| T-05-03-04 | Tampering / Elevation | SCAN_ENGINE_TEST_FAULT seam reachable in prod | high | mitigate | Seam is inert unless `NODE_ENV !== 'production'` (existing HIGH-02); the forced-failure case runs the worker under `nodeEnv:'test'` ONLY; the happy path runs `production` with real adapters |
| T-05-03-05 | Denial of service | unbounded harness/container timeouts hang CI | medium | mitigate | Every subprocess uses a finite bounded timeout (reused constants); one-shot OOM container is `docker rm -f`'d in a finally |
| T-05-03-SC | Tampering | pinned redis:7-alpine / Trivy images used by the harness | medium | mitigate | Pinned tags reused from the in-repo feasibility probe; `npm ci` lockfile install; no floating `latest` |
</threat_model>

<verification>
- `npm run test:acceptance --workspace apps/api` proves criteria #1, #3, #5 (Docker-gated cases skip-with-reason when Docker is absent, never fail closed).
- `npm run test:oom:container --workspace apps/api` proves criterion #2 (`false 0` from docker inspect) or records a feasibility skip.
- `scan-engine.yml` parses with the new feasibility-gated jobs wired; `memory.yml` is unchanged (`git diff --exit-code`); `CI-CONTRACT.md` job identifiers match the workflow (criterion #4, OPS-05).
- The Node-22 memory proof (`memory.yml`) remains a required gate (criterion #4 / #5b).
</verification>

<success_criteria>
- OPS-05: automated suite covers parser unit + scan API contract integration + acceptance; CI runs lint + type-check + tests and fails on any failure.
- Criterion #1 (acceptance + cleanup on success/failure), #2 (in-container OOMKilled:false AND exit 0), #3 (scanId correlation across processes), #4 (CI gating + memory proof required), #5 (index.js boots @150 + 500MB parse @150) all proven or feasibility-gated with recorded reason.
- OPS-02 end-to-end: the acceptance flow exercises the real compiled stack over disposable infra.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-ops-assignment-acceptance/05-03-SUMMARY.md` when done.
</output>