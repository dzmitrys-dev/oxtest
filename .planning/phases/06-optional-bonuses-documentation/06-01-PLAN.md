---
phase: 06-optional-bonuses-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/graphql/vulnerability.model.ts
  - apps/api/src/graphql/scan.model.ts
  - apps/api/src/graphql/scan-graphql.mapper.ts
  - apps/api/src/graphql/scan.resolver.ts
  - apps/api/src/graphql/scan.resolver.spec.ts
  - apps/api/src/app.module.ts
  - apps/api/package.json
  - package.json
autonomous: true
requirements: [API-01, API-02]
tags: [graphql, mercurius, nestjs, resolver, ssrf, bonus-b]

must_haves:
  truths:
    - "A GraphQL query scan(id) returns type Scan { id: ID!, status: String!, criticalVulnerabilities: [Vulnerability] } by delegating to ScanService.get(id) — the exact locked API-01 schema, code-first via MercuriusDriver (D-02, API-01)"
    - "An unknown id resolves to null (not an error) because ScanService.get returns null — REST 404 parity for the query path (D-06)"
    - "A GraphQL mutation enqueueScan(repoUrl) enqueues a scan via ScanService.enqueue and returns the queued Scan — parity with POST /api/scan (D-02, API-02)"
    - "enqueueScan validates repoUrl with the SAME parseGithubUrl allowlist as REST and enqueues ONLY the canonical https://github.com/{owner}/{repo} URL, rejecting every non-github/non-https/userinfo/ssh/git-scheme input BEFORE ScanService.enqueue runs (Pitfall 5, SCAN-02 parity, T-06-01)"
    - "criticalVulnerabilities is populated only when status is Finished, else undefined/empty — the raw domain Scan is never spread (repoUrl/createdAt/updatedAt never leak on the wire, D-06)"
    - "GraphiQL is reachable at /graphiql in all environments incl. container for reviewer explorability (D-05)"
    - "The GraphQL schema builds at app bootstrap: node dist/index.js still boots to 'API HTTP listener ready' with GraphQLModule registered (schema-build regression gate)"
    - "GraphQLModule is registered in AppModule ONLY, never WorkerModule — the memory-critical worker heap stays free of GraphQL code (two-entrypoint discipline)"
  artifacts:
    - "apps/api/src/graphql/vulnerability.model.ts — @ObjectType() VulnerabilityModel (the 6 stored fields)"
    - "apps/api/src/graphql/scan.model.ts — @ObjectType() ScanModel (id/status:String!/criticalVulnerabilities)"
    - "apps/api/src/graphql/scan-graphql.mapper.ts — toScanModel(scan): status-switched domain→model mapper"
    - "apps/api/src/graphql/scan.resolver.ts — @Resolver ScanResolver: @Query scan, @Mutation enqueueScan"
    - "apps/api/src/graphql/scan.resolver.spec.ts — Jest-landmine-safe resolver unit test (mocked ScanService)"
    - "GraphQL quartet pinned in apps/api/package.json + root graphql override"
  key_links:
    - "ScanResolver → ScanService.enqueue (scan.service.ts:34) / ScanService.get (scan.service.ts:55) — sole collaborator (ARCH-01)"
    - "ScanResolver.enqueueScan → parseGithubUrl (src/http/validation/github-url.ts) — one fail-closed allowlist across both transports"
    - "GraphQLModule.forRoot<MercuriusDriverConfig> registered in AppModule imports → mounts /graphql + /graphiql on the existing Fastify listener"
---

<objective>
Deliver Bonus B: a code-first GraphQL surface (NestJS MercuriusDriver) exposing the locked API-01 `scan(id)` query and an `enqueueScan(repoUrl)` mutation, both thin adapters delegating to the existing `ScanService` — no reimplementation of scan logic (D-02). The mutation reuses the REST SSRF/command-injection allowlist (`parseGithubUrl`) and enqueues only the canonical URL, closing the second-path hole (Pitfall 5, SCAN-02 parity). GraphQL + GraphiQL mount on the SAME Fastify process/port as REST (zero second-listener overhead), in `AppModule` only — the worker stays heap-lean.

Purpose: satisfy phase success criterion #1 (GraphQL delegates to the same ScanService as REST) and requirements API-01/API-02, additively, without touching the parser/worker/REST contracts.
Output: `apps/api/src/graphql/*` (2 models, 1 mapper, 1 resolver, 1 spec), `AppModule` GraphQL registration, pinned GraphQL quartet.
</objective>

<execution_context>
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/workflows/execute-plan.md
@/home/dzmitry/Coding/oxtest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-optional-bonuses-documentation/06-CONTEXT.md
@.planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md
@.planning/phases/06-optional-bonuses-documentation/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the GraphQL quartet and create the code-first models + mapper</name>
  <files>apps/api/src/graphql/vulnerability.model.ts, apps/api/src/graphql/scan.model.ts, apps/api/src/graphql/scan-graphql.mapper.ts, apps/api/package.json, package.json</files>
  <read_first>
    - apps/api/src/domain/vulnerability.types.ts (authoritative 6-field Vulnerability shape — NO fixed-version field, D-08)
    - apps/api/src/domain/scan.types.ts (Scan + ScanStatus enum — the four states)
    - apps/api/src/http/dto/scan-response.ts (toScanResponse — the status-switched mapper this mirrors)
    - apps/api/package.json (current deps + versions; TS 6.0.3, Nest 11.1.28, fastify 5.10.0)
    - package.json (monorepo root — add graphql override here)
    - .planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md (Standard Stack versions; Pitfall 1 graphql@16 pin; Pattern 1 & 2 code samples)
  </read_first>
  <action>
    Install the pinned GraphQL quartet into apps/api verbatim (the versions are locked by RESEARCH Standard Stack; do NOT let any resolve to graphql@17): @nestjs/graphql@13.4.2, @nestjs/mercurius@13.4.2, mercurius@16.9.0, graphql@16.14.2 as apps/api dependencies. Add a root package.json "overrides" entry pinning graphql to 16.14.2 so no transitive dependency pulls graphql@17 anywhere in the monorepo (Pitfall 1). After install, confirm `npm ls graphql` shows only 16.14.2.

    Create apps/api/src/graphql/vulnerability.model.ts: an @ObjectType() class VulnerabilityModel with exactly six @Field() members mirroring the stored Vulnerability shape — vulnerabilityId, pkgName, installedVersion, severity, title, primaryUrl (all string; severity typed as string, value always 'CRITICAL'). Do NOT add any field that is not in the stored domain Vulnerability (the persisted shape has no fix-version member — adding one would force a parser change, forbidden by D-08). Import decorators only from @nestjs/graphql.

    Create apps/api/src/graphql/scan.model.ts: an @ObjectType() class ScanModel with `@Field(() => ID) id: string`, `@Field() status: string` (String! — a plain string, NOT a GraphQL enum, per the locked API-01 schema; map the ScanStatus enum value to its string), and `@Field(() => [VulnerabilityModel], { nullable: true }) criticalVulnerabilities?: VulnerabilityModel[]`.

    Do NOT decorate the framework-free domain types in src/domain/* — they carry the "no NestJS/GraphQL imports" header (D-03). The models are separate DECORATED mirrors (anti-pattern to import @nestjs/graphql into src/domain).

    Create apps/api/src/graphql/scan-graphql.mapper.ts exporting `toScanModel(scan: Scan): ScanModel`, mirroring toScanResponse exactly: switch on ScanStatus; set model.status to the enum value (which IS the wire string); populate criticalVulnerabilities only when status is Finished (from scan.vulnerabilities ?? []), leave undefined otherwise; never spread the raw Scan (drop repoUrl/createdAt/updatedAt — D-06 no-raw-leak parity). Import the Scan domain type type-only.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit && npm ls graphql --workspace apps/api 2>/dev/null | grep -q '16.14.2'</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls graphql` resolves to exactly 16.14.2 (no 17.x anywhere); root package.json has an overrides pin for graphql (Pitfall 1).
    - VulnerabilityModel exposes exactly the six stored fields; no extra field is added (D-08 — parser untouched).
    - ScanModel.status is String! (a plain string @Field), NOT a GraphQL enum (API-01 locked schema).
    - toScanModel populates criticalVulnerabilities only for Finished; Failed/Queued/Scanning leave it undefined; raw Scan fields (repoUrl/createdAt/updatedAt) never appear on the model (D-06).
    - `tsc --noEmit` passes (strict, no `any`).
    - No file under src/domain/ imports @nestjs/graphql.
  </acceptance_criteria>
  <done>Pinned GraphQL deps installed (graphql locked to 16.14.2 incl. root override); VulnerabilityModel, ScanModel, and toScanModel exist and type-check; domain types remain decorator-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thin ScanResolver with SSRF-parity mutation + AppModule GraphQL registration</name>
  <files>apps/api/src/graphql/scan.resolver.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/http/scan.controller.ts (the REST edge — mirror its delegation shape 1:1)
    - apps/api/src/http/validation/github-url.ts (parseGithubUrl — the pure validator to reuse)
    - apps/api/src/http/validation/github-url.pipe.ts (the canonical-form contract to reproduce: reject null, enqueue https://github.com/{owner}/{repo})
    - apps/api/src/scan/scan.service.ts (enqueue:34 / get:55 — the delegation targets)
    - apps/api/src/app.module.ts (current AppModule — add-only)
    - apps/api/src/worker.module.ts (confirm GraphQL/static are NOT added here)
    - .planning/phases/06-optional-bonuses-documentation/06-RESEARCH.md (Pattern 1 resolver, Pattern 2 MercuriusDriver registration, Pitfall 5 SSRF parity)
  </read_first>
  <behavior>
    - scan(id) delegates to ScanService.get(id); returns toScanModel(scan) when found, null when the service returns null (D-06 query 404-parity).
    - enqueueScan(repoUrl): calls parseGithubUrl(repoUrl); on null throws a client error (must reject before ScanService.enqueue runs); on success enqueues the CANONICAL https://github.com/{owner}/{repo} and returns toScanModel of the queued Scan.
    - enqueueScan is called exactly once with the canonical URL (never the raw input string) for a valid github URL.
    - Non-github/non-https/ssh(git@)/git://file:// and userinfo/port inputs are rejected without calling enqueue.
  </behavior>
  <action>
    Create apps/api/src/graphql/scan.resolver.ts: `@Resolver(() => ScanModel) export class ScanResolver`, constructor injecting ONLY `private readonly scans: ScanService` (sole collaborator, ARCH-01). Add `@Query(() => ScanModel, { nullable: true }) async scan(@Args('id', { type: () => ID }) id: string): Promise<ScanModel | null>` that awaits this.scans.get(id) and returns null when the scan is null, else toScanModel(scan). Add `@Mutation(() => ScanModel) async enqueueScan(@Args('repoUrl') repoUrl: string): Promise<ScanModel>` that calls parseGithubUrl(repoUrl); if it returns null throw a GraphQL-surfacing error whose message matches the REST wording ("repoUrl must be an https://github.com/{owner}/{repo} URL"); otherwise build the canonical `https://github.com/${parsed.owner}/${parsed.repo}` and return toScanModel(await this.scans.enqueue(canonical)). Import parseGithubUrl from ../http/validation/github-url (the pure validator, NOT the pipe). Keep the resolver's imports limited to @nestjs/graphql, ScanService, parseGithubUrl, the mapper, and the models — no fs/child_process/@nestjs/bullmq/engine/parser imports (same import discipline as scan.controller.ts).

    Modify apps/api/src/app.module.ts: add to imports `GraphQLModule.forRoot<MercuriusDriverConfig>({ driver: MercuriusDriver, autoSchemaFile: true, graphiql: true })` (import GraphQLModule from @nestjs/graphql, MercuriusDriver + MercuriusDriverConfig from @nestjs/mercurius) — graphiql:true keeps GraphiQL at /graphiql in ALL environments incl. container (D-05). Add ScanResolver to the providers array. Do NOT modify worker.module.ts — GraphQL is AppModule-only (worker heap discipline). Leave ScanModule/ScanController/HealthController exactly as they are.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit && npm run build --workspace apps/api && npm run test:selftest --workspace apps/api</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build --workspace apps/api` compiles; `node dist/index.js` boots to 'API HTTP listener ready' (test:selftest passes) — proving the code-first schema builds at bootstrap with GraphQLModule registered.
    - ScanResolver.enqueueScan rejects a null parseGithubUrl result BEFORE calling ScanService.enqueue and, on success, calls enqueue with the canonical https://github.com/{owner}/{repo} URL (Pitfall 5 / T-06-01) — asserted by the Task 3 spec.
    - ScanResolver.scan returns null (not throw) for an id ScanService.get resolves to null (D-06).
    - app.module.ts registers GraphQLModule (MercuriusDriver, graphiql:true) and adds ScanResolver to providers; worker.module.ts is unchanged.
    - The resolver imports no fs/child_process/@nestjs/bullmq/engine/parser modules (ARCH-01).
  </acceptance_criteria>
  <done>ScanResolver delegates scan/enqueueScan to ScanService with canonical-URL SSRF parity; AppModule mounts GraphQL + GraphiQL on the existing Fastify listener; the API boots with the schema built; worker untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Jest-landmine-safe resolver unit test</name>
  <files>apps/api/src/graphql/scan.resolver.spec.ts</files>
  <read_first>
    - apps/api/src/http/scan.controller.spec.ts (exact analog — copy the mock-ScanService strategy, baseScan/critical fixtures, and import-guard idiom)
    - apps/api/src/graphql/scan.resolver.ts (the unit under test)
    - .planning/phases/06-optional-bonuses-documentation/06-PATTERNS.md ("Mock-service unit test" — Jest landmine avoidance)
    - .planning/STATE.md (the @swc/core + @nestjs/bullmq miette Jest panic — reproduces on Node 22 and 24)
  </read_first>
  <action>
    Create apps/api/src/graphql/scan.resolver.spec.ts mirroring scan.controller.spec.ts: `import 'reflect-metadata'` FIRST; `import type { ScanService }` (type-only — never the ScanModule); reuse the baseScan(overrides) and critical Vulnerability fixture builders. Construct the resolver directly with a hand-mocked service: `const service = { enqueue, get } as unknown as ScanService; new ScanResolver(service)` where enqueue/get are jest.fn. NEVER import ScanModule / AppModule / @nestjs/bullmq (the @swc miette panic aborts Jest — this is why the spec constructs the resolver by hand).

    Cover: (1) scan(id) with a Finished scan returns toScanModel output incl. criticalVulnerabilities; (2) scan(id) returns null when get resolves null (D-06); (3) enqueueScan with a valid URL calls enqueue exactly once with the CANONICAL https://github.com/{owner}/{repo} (assert canonicalization: e.g. input with trailing .git or www. host normalizes) and returns the mapped model; (4) enqueueScan with a non-github/ssh/git:// URL rejects (throws) WITHOUT calling enqueue (T-06-01 SSRF parity). Add an import-guard test (mirroring scan.controller.spec.ts) reading scan.resolver.ts source and asserting it contains none of node:fs/fs/node:child_process/child_process/execa/@nestjs/bullmq and no engine/ or report-parser specifiers.
  </action>
  <verify>
    <automated>cd apps/api && npm test --workspace apps/api</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` runs to completion (no Rust/miette panic) and the resolver spec passes — proving the mock-only strategy dodges the @nestjs/bullmq Jest landmine.
    - Test asserts enqueue is called once with the canonical URL and never called for an invalid URL (T-06-01).
    - Test asserts scan(id) returns null on a null service result (D-06).
    - Import-guard test passes: scan.resolver.ts imports none of the forbidden fs/child_process/@nestjs/bullmq/engine/parser modules (ARCH-01).
  </acceptance_criteria>
  <done>scan.resolver.spec.ts passes under Jest without importing ScanModule/@nestjs/bullmq; delegation, canonicalization, SSRF rejection, null-parity, and the import-guard are all covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GraphQL client → enqueueScan mutation | Untrusted repoUrl crosses into the repo cloner — a SECOND path to the same sink REST guards |
| Internet → /graphql, /graphiql | Introspection + interactive playground surface exposed on the Fastify listener |
| npm registry → build | Newly installed GraphQL quartet packages enter the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering / EoP | enqueueScan resolver (repoUrl → cloner) | critical | mitigate | Reuse `parseGithubUrl` in the resolver; reject on null; enqueue ONLY the canonical `https://github.com/{owner}/{repo}` — one fail-closed allowlist across REST + GraphQL (SCAN-02 parity, Pitfall 5). Asserted by scan.resolver.spec.ts. |
| T-06-02 | Info Disclosure | GraphiQL + introspection at /graphiql (all envs, D-05) | low | accept | Deliberate demo choice for reviewer explorability; single-tenant take-home with no auth/data-at-rest to protect. Documented as an owned trade-off in ONBOARDING (Plan 04); a real deployment would gate GraphiQL/introspection behind env + auth. |
| T-06-05 | Denial of Service | GraphQL query depth/complexity abuse | low | accept | Schema is two flat types with no recursion/nesting; complexity limiting offers no meaningful protection at this scope — noted as a v2 consideration in ONBOARDING. |
| T-06-SC | Tampering | npm installs (@nestjs/graphql, @nestjs/mercurius, mercurius, graphql) | high | mitigate | Install verbatim pinned versions from RESEARCH Standard Stack + root graphql@16.14.2 override; RESEARCH Package Legitimacy Audit lists all as Approved (SUS entries are "too-new" false positives on mature, high-download, official-repo packages; no [SLOP], no [ASSUMED], no postinstall scripts) — no human legitimacy checkpoint required, but pins are applied exactly and `npm ls graphql` is verified. |
</threat_model>

<artifacts_produced>
New symbols/types/files this plan produces:
- `VulnerabilityModel` (@ObjectType) — apps/api/src/graphql/vulnerability.model.ts
- `ScanModel` (@ObjectType) — apps/api/src/graphql/scan.model.ts
- `toScanModel(scan: Scan): ScanModel` — apps/api/src/graphql/scan-graphql.mapper.ts
- `ScanResolver` (@Resolver with `scan` @Query + `enqueueScan` @Mutation) — apps/api/src/graphql/scan.resolver.ts
- `scan.resolver.spec.ts` — apps/api/src/graphql/scan.resolver.spec.ts
- GraphQL SDL: `type Scan { id: ID!, status: String!, criticalVulnerabilities: [Vulnerability] }` + `type Vulnerability {...6 fields}` + `Query.scan(id: ID!)` + `Mutation.enqueueScan(repoUrl: String!)`
- GraphQL HTTP endpoints on the existing Fastify listener: POST/GET `/graphql`, `/graphiql`
- Pinned deps: @nestjs/graphql@13.4.2, @nestjs/mercurius@13.4.2, mercurius@16.9.0, graphql@16.14.2 (+ root override)
</artifacts_produced>

<verification>
- `npm ls graphql` → 16.14.2 only (no 17.x); root overrides present.
- `npm run typecheck --workspace apps/api` (tsc --noEmit) passes strict, no `any`.
- `npm run build --workspace apps/api` compiles the graphql/ sources.
- `npm run test:selftest --workspace apps/api` — dist/index.js boots to the listener marker with the code-first schema built (schema-build regression gate + criterion #5a stays green).
- `npm test --workspace apps/api` — resolver spec passes with NO miette panic (Jest landmine avoided).
- Worker unchanged: worker.module.ts has no GraphQL/static imports.
</verification>

<success_criteria>
GraphQL `scan(id)` query and `enqueueScan` mutation both delegate to the same `ScanService` as REST (phase criterion #1, API-01/API-02); the mutation enforces the REST SSRF/injection allowlist and enqueues only canonical URLs; GraphiQL is live at /graphiql; the API boots with the schema built; the worker heap is untouched.
</success_criteria>

<output>
Create `.planning/phases/06-optional-bonuses-documentation/06-01-SUMMARY.md` when done.
</output>
