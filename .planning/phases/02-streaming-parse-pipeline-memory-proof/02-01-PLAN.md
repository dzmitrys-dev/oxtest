---
phase: 02-streaming-parse-pipeline-memory-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/api/src/parser/report-parser.ts, apps/api/src/parser/report-parser.spec.ts, apps/api/fixtures/known-severity-mix.json, apps/api/package.json, package-lock.json, apps/api/eslint.config.mjs, .gitignore]
autonomous: false
requirements: [ENGINE-05, MEM-01, MEM-04]
must_haves:
  truths:
    - "ReportParser emits only mapped CRITICAL vulnerabilities from nested Trivy Results without assembling a Result."
    - "The committed mixed-severity fixture proves exact CRITICAL filtering and zero non-CRITICAL output."
    - "The parser path has a mechanical guard against full-report materialization while permitting streamValues."
  artifacts:
    - path: apps/api/src/parser/report-parser.ts
      provides: Framework-free ReportParser async generator using the deep leaf Pick path
    - path: apps/api/src/parser/report-parser.spec.ts
      provides: Deterministic parser regression test
    - path: apps/api/fixtures/known-severity-mix.json
      provides: Committed mixed-severity Trivy-shaped input
    - path: apps/api/eslint.config.mjs
      provides: Parser-scoped forbidden API rules
  key_links:
    - from: apps/api/src/parser/report-parser.ts
      to: apps/api/src/domain/trivy-report.types.ts and apps/api/src/domain/vulnerability.types.ts
      via: Typed PascalCase-to-camelCase CRITICAL mapping
    - from: apps/api/src/parser/report-parser.spec.ts
      to: apps/api/fixtures/known-severity-mix.json
      via: Async-generator iteration
---

<objective>
Build and lock the standalone leaf-streaming parser contract, its deterministic correctness proof, and the static guard that prevents accidental full-report buffering.

Purpose: Establish ENGINE-05/MEM-04 before memory instrumentation, while preserving the CommonJS configuration and keeping the parser out of runtime modules.
Output: Exported ReportParser, correctness fixture and Jest test, exact stream dependencies, parser lint restrictions, and generated-fixture command/ignore foundation.
</objective>

<execution_context>
@/home/dzmitry/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/dzmitry/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md
@.planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md
@.planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md
@apps/api/src/domain/trivy-report.types.ts
@apps/api/src/domain/vulnerability.types.ts
@apps/api/package.json
@apps/api/eslint.config.mjs
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve pinned stream package legitimacy</name>
  <files>apps/api/package.json, package-lock.json</files>
  <read_first>apps/api/package.json; package-lock.json; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md</read_first>
  <acceptance_criteria>Approval is recorded only after apps/api/package.json names stream-json 2.1.0 and stream-chain 3.6.3, package-lock.json resolves those exact versions, and the workspace engine remains in the Node 22-only range.</acceptance_criteria>
  <action>Confirm the dependency plan uses the audited CommonJS releases stream-json 2.1.0 and stream-chain 3.6.3, not an unpinned or ESM-only major, per D-01 and the resolved research. Follow the existing dependency layout analog in apps/api/package.json documented by 02-PATTERNS.md.</action>
  <verify><automated>test "$(node -p "require('./apps/api/package.json').engines.node")" = ">=22 <23"</automated></verify>
  <done>Exact audited versions are approved before installation.</done>
  <resume-signal>Type approved to continue, or describe the package/version concern.</resume-signal>
</task>
<task type="auto">
  <name>Task 2: Implement leaf-streaming parser and correctness fixture</name>
  <files>apps/api/src/parser/report-parser.ts, apps/api/src/parser/report-parser.spec.ts, apps/api/fixtures/known-severity-mix.json, apps/api/package.json, package-lock.json, apps/api/eslint.config.mjs, .gitignore</files>
  <read_first>apps/api/src/parser/report-parser.ts; apps/api/src/parser/report-parser.spec.ts; apps/api/fixtures/known-severity-mix.json; apps/api/package.json; package-lock.json; apps/api/eslint.config.mjs; .gitignore; apps/api/src/scan/scan.store.ts; apps/api/src/domain/trivy-report.types.ts; apps/api/src/domain/vulnerability.types.ts; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md</read_first>
  <acceptance_criteria>Typecheck, focused Jest, and lint pass; the fixture-driven test yields the exact mapped CRITICAL set and no HIGH or LOW values; source contains the deep Results index/Vulnerabilities index Pick plus streamValues; parser source contains no report-wide read, JSON.parse, or toArray; dependencies resolve to 2.1.0 and 3.6.3; no ScanModule or entrypoint imports ReportParser.</acceptance_criteria>
  <action>Per D-01, D-02, D-04, D-07, and resolved D-08, pin stream-json@2.1.0 and stream-chain@3.6.3; implement exported ReportParser.parse(reportPath: string): AsyncIterable<Vulnerability> as a native async generator over chain([createReadStream, parser, pick({ filter: /^Results\.\d+\.Vulnerabilities\.\d+$/ }), streamValues, CRITICAL filter]). Follow the framework-free named-class/domain-service convention of apps/api/src/scan/scan.store.ts as assigned in 02-PATTERNS.md, while using the corrected leaf-path research skeleton. Map the existing TrivyVulnerability fields into the frozen Vulnerability type, yield one value at a time, propagate stream errors, and do not wire the parser into ScanModule or either entrypoint. Create the mixed and deliberately skewed committed fixture and exact CRITICAL-only Jest assertions. Add parser-scoped ESLint restrictions for JSON.parse, fs.readFile/readFileSync, and toArray while explicitly permitting streamValues. Add gen:fixture and a narrow generated-large-fixture ignore rule.</action>
  <verify><automated>npm run typecheck --workspace apps/api && npm test --workspace apps/api -- --runInBand src/parser/report-parser.spec.ts && npm run lint --workspace apps/api</automated>
    <automated>node -e "const p=require('./apps/api/package.json'); if(p.dependencies['stream-json']!=='2.1.0'||p.dependencies['stream-chain']!=='3.6.3') process.exit(1)"</automated></verify>
  <done>Parser, exact fixture test, dependency pins, static guard, command entry, and ignore rule exist and pass.</done>
</task>
<task type="auto">
  <name>Task 3: Verify parser guard and runtime boundary</name>
  <files>apps/api/src/parser/report-parser.ts, apps/api/src/parser/report-parser.spec.ts, apps/api/eslint.config.mjs</files>
  <read_first>apps/api/src/parser/report-parser.ts; apps/api/src/parser/report-parser.spec.ts; apps/api/eslint.config.mjs; apps/api/src/scan/scan.store.ts; apps/api/src/index.ts; apps/api/src/worker.ts; apps/api/src/scan/scan.module.ts; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-PATTERNS.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-RESEARCH.md; .planning/phases/02-streaming-parse-pipeline-memory-proof/02-CONTEXT.md</read_first>
  <acceptance_criteria>Lint passes; one source assertion independently finds the exact deep Pick path; a second independently finds streamValues; parser source has no forbidden report-wide APIs; and runtime-boundary checks find no ReportParser import in index.ts, worker.ts, or scan.module.ts.</acceptance_criteria>
  <action>Exercise lint and inspect the runtime boundary. Follow the framework-free parser boundary analog in apps/api/src/scan/scan.store.ts from 02-PATTERNS.md, and compare CLI/runtime isolation with apps/api/src/worker.ts. Confirm the parser imports only streaming/file primitives and frozen domain types, no runtime entrypoint or ScanModule imports it, and the guard covers report-wide reads/parsing and accumulation without rejecting the corrected leaf path or streamValues. Use separate literal source checks: one checks the exact deep Results index/Vulnerabilities index Pick path, and another checks streamValues usage; do not combine them in an escaped OR grep.</action>
  <verify><automated>npm run lint --workspace apps/api && ! grep -Eq 'readFileSync|readFile|JSON\.parse|\.toArray\(' apps/api/src/parser/report-parser.ts && ! grep -R -E 'ReportParser' apps/api/src/index.ts apps/api/src/worker.ts apps/api/src/scan/scan.module.ts</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('apps/api/src/parser/report-parser.ts','utf8'); if(!s.includes('Results.\\\\d+.Vulnerabilities.\\\\d+')) process.exit(1)"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('apps/api/src/parser/report-parser.ts','utf8'); if(!s.includes('streamValues')) process.exit(1)"</automated></verify>
  <done>Independent reliable checks prove the deep leaf path and streamValues separately, and no later-phase wiring exists.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Report file → parser | Malformed, deep, or large input crosses into parsing. |
| CLI arguments → fixture tooling | Size/path controls affect disk and CPU. |
| Registry → build | Parser packages enter the supply chain. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | ReportParser | high | mitigate | Leaf regex Pick, streamValues, parser errors, and mixed/skewed regression fixture. |
| T-02-02 | Denial of Service | Parser path/depth | high | mitigate | One-object streaming and static no-materialization guard. |
| T-02-03 | Tampering | npm dependencies | high | mitigate | Blocking approval and exact lockfile versions. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy checkpoint and exact versions. |
</threat_model>

<verification>Run focused Jest, typecheck, lint, exact dependency assertions, independent leaf-path and streamValues checks, and the runtime-boundary check.</verification>
<success_criteria>A tested framework-free parser emits only CRITICAL vulnerabilities one at a time using the corrected deep Pick/streamValues path, with exact CJS pins and no runtime wiring.</success_criteria>
<output>Create .planning/phases/02-streaming-parse-pipeline-memory-proof/02-01-SUMMARY.md when done.</output>
